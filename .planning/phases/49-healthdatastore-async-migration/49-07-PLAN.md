---
phase: 49-healthdatastore-async-migration
plan: 07
type: execute
wave: 3
depends_on: ["49-01", "49-02", "49-03", "49-04", "49-05", "49-06"]
files_modified:
  - GooseSwift/HealthDataStore.swift
  - GooseSwift/GooseRustBridge.swift
  - GooseSwift/AppShellView.swift
  - GooseSwift/HealthView.swift
  - GooseSwift/HealthDashboardViews.swift
  - GooseSwift/HealthRecoveryStressViews.swift
  - GooseSwift/HealthSleepOverviewViews.swift
  - GooseSwift/HealthMetricFamilyStrainViews.swift
  - GooseSwift/SleepV2ScheduleViews.swift
  - GooseSwift/SleepBridgeViews.swift
  - GooseSwift/HomeDashboardView.swift
  - GooseSwift/MoreRawExportViews.swift
  - GooseSwift/MoreDataStore+Validation.swift
  - GooseSwift/CoachView.swift
autonomous: false
requirements: [ASYNC-01, ASYNC-02]
must_haves:
  truths:
    - "packetInputQueue and heartRateTimelineQueue are removed from HealthDataStore.swift"
    - "refreshBridgeCatalogs is async and awaits bridge.requestAsync; refreshPacketInputsAfterCapture uses Task+Task.sleep cancellation instead of DispatchWorkItem"
    - "All external view/model callers of now-async run/refresh methods are wrapped in Task await"
    - "Zero non-awaited bridge.request / bridge.requestValue occurrences remain anywhere in GooseSwift/"
    - "Dashboards (Recovery V2, Sleep V2, Esforco) populate after migration in the simulator"
  artifacts:
    - path: "GooseSwift/HealthDataStore.swift"
      provides: "queue-free async refreshBridgeCatalogs + Task-based debounce; no DispatchQueue health queues"
      contains: "func refreshBridgeCatalogs() async"
    - path: "GooseSwift/GooseRustBridge.swift"
      provides: "sync request/requestValue removed; requestAsync is the sole bridge entry"
  key_links:
    - from: "SwiftUI view callers"
      to: "store run/refresh async methods"
      via: "Task await wrapper"
      pattern: "Task \\{[^}]*await"
    - from: "refreshBridgeCatalogs"
      to: "bridge.requestAsync"
      via: "3 awaited catalog calls"
      pattern: "await .*requestAsync"
---

<objective>
Final cleanup wave (depends on all wave-2 plans). Closes ASYNC-01 and ASYNC-02:

1. Migrate the remaining bridge calls in HealthDataStore.swift (refreshBridgeCatalogs — 3 calls at lines 218-220) to async + await bridge.requestAsync.
2. Convert refreshPacketInputsAfterCapture from DispatchWorkItem.cancel() debounce to a stored Task with Task.sleep(for: .seconds(0.8)) cancellation (RISK-04). Convert refreshHeartRateTimeline (uses heartRateTimelineQueue, no bridge) to async (RISK-05 / A4).
3. Finalize refreshSleepAfterBandSync into a sequential async chain (await runPacketInputs; await runSleepScore; await runSleepStaging) and remove any temporary shims added in 49-02 (RISK-03).
4. Remove packetInputQueue and heartRateTimelineQueue declarations from HealthDataStore.swift (D-05) plus packetInputRefreshWorkItem (replaced by packetInputRefreshTask).
5. Wrap every external SwiftUI/model caller of a now-async method in Task await (D-03, Pattern 3) — full list in RESEARCH.md "External Callers to Migrate".
6. Final cleanup of GooseRustBridge: remove the now-unused sync request/requestValue, since the global grep audit confirms zero sync callers remain (Claude's Discretion — additive-then-remove).
7. Full build (D-08a) + simulator smoke test (D-08b).

Purpose: Eliminate the last bridge calls, remove all GCD health queues, fix all external callers, and verify end-to-end.
Output: queue-free async HealthDataStore; async-only GooseRustBridge; all callers wrapped; green build + verified dashboards.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-healthdatastore-async-migration/49-CONTEXT.md
@.planning/phases/49-healthdatastore-async-migration/49-RESEARCH.md
@.planning/phases/49-healthdatastore-async-migration/49-01-SUMMARY.md
@.planning/phases/49-healthdatastore-async-migration/49-02-SUMMARY.md
@.planning/phases/49-healthdatastore-async-migration/49-03-SUMMARY.md
@.planning/phases/49-healthdatastore-async-migration/49-04-SUMMARY.md
@.planning/phases/49-healthdatastore-async-migration/49-05-SUMMARY.md
@.planning/phases/49-healthdatastore-async-migration/49-06-SUMMARY.md
@GooseSwift/GooseRustBridge.swift
@GooseSwift/HealthDataStore.swift
</context>

## Artifacts this plan produces

- `HealthDataStore.refreshBridgeCatalogs() async` (3 awaited bridge calls)
- `HealthDataStore.refreshHeartRateTimeline() async` (queue-free)
- `HealthDataStore.refreshPacketInputsAfterCapture()` rewritten to a Task-debounce
- `HealthDataStore.packetInputRefreshTask: Task<Void, Error>?` (replaces packetInputRefreshWorkItem)
- `HealthDataStore.refreshSleepAfterBandSync(packetCount:) async` (sequential await chain, no completion closure)
- Removal of packetInputQueue and heartRateTimelineQueue from HealthDataStore.swift
- Removal of sync request/requestValue from GooseRustBridge.swift (async-only bridge)
- Task await wrappers at all external caller sites (14+ per RESEARCH.md)

<tasks>

<task type="auto">
  <name>Task 1: Migrate remaining HealthDataStore.swift bridge calls, remove queues, add Task debounce</name>
  <files>GooseSwift/HealthDataStore.swift</files>
  <read_first>
    - GooseSwift/HealthDataStore.swift (whole file — refreshBridgeCatalogs at ~line 213 with 3 bridge calls at lines 218-220; refreshPacketInputsAfterCapture using packetInputRefreshWorkItem at line 48; refreshHeartRateTimeline using heartRateTimelineQueue; packetInputQueue line 53, heartRateTimelineQueue line 54; refreshSleepAfterBandSync; refreshPacketInputsIfNeeded; loadBridgeCatalogsIfNeeded)
    - GooseSwift/HealthDataStore+PacketInputs.swift (async packetInputBridgeReports from 49-02)
    - .planning/phases/49-healthdatastore-async-migration/49-RESEARCH.md (RISK-03, RISK-04, RISK-05)
    - .planning/phases/49-healthdatastore-async-migration/49-02-SUMMARY.md (any shim notes to finalize)
  </read_first>
  <action>
    a) refreshBridgeCatalogs: convert to func refreshBridgeCatalogs() async. Remove the packetInputQueue.async wrapper. Replace the 3 try bridge.request calls (methods metrics.built_in_definitions, metrics.reference_definitions, metrics.default_preferences) with try await bridge.requestAsync. Mutate the catalog @Observable properties directly on self after the awaits. Preserve existing catalogStatus and catalogSource error handling.

    b) refreshHeartRateTimeline: convert to func refreshHeartRateTimeline() async. Remove the heartRateTimelineQueue.async wrapper — call heartRateSeriesStore.timelineSnapshot() directly (in-memory, no bridge), then mutate the timeline @Observable property on self (RISK-05 / A4).

    c) refreshPacketInputsAfterCapture: replace the DispatchWorkItem debounce. Change the stored property packetInputRefreshWorkItem: DispatchWorkItem? (line 48) to packetInputRefreshTask: Task<Void, Error>?. New body: cancel the prior task, then assign a new Task that does try await Task.sleep(for: .seconds(0.8)), guards on Task.isCancelled, then awaits self.runPacketInputs(). Keep the 0.8s debounce window (RISK-04). Use weak self in the Task closure.

    d) refreshSleepAfterBandSync(packetCount:): convert to func refreshSleepAfterBandSync(packetCount: Int) async. Replace the old runPacketInputs(completion:) closure chain with a sequential await chain: set the start status, await runPacketInputs(), await runSleepScore(), await runSleepStaging(), set the done status (RISK-03). Remove any temporary shim added in 49-02.

    e) refreshPacketInputsIfNeeded and loadBridgeCatalogsIfNeeded: convert to async (they call now-async runPacketInputs / refreshBridgeCatalogs); chain with await. Any other in-file caller of a now-async method must use await (if async) or Task await wrapper (if it must stay sync).

    f) Remove the let packetInputQueue = DispatchQueue(...) (line 53) and let heartRateTimelineQueue = DispatchQueue(...) (line 54) declarations entirely (D-05). Grep must then show zero references to either identifier in the file.

    Capture all @MainActor state into locals before each first await. Never assign self.x inside a Task.detached body.
  </action>
  <verify>
    <automated>grep -c "packetInputQueue\|heartRateTimelineQueue\|DispatchWorkItem" GooseSwift/HealthDataStore.swift</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "packetInputQueue\|heartRateTimelineQueue" GooseSwift/HealthDataStore.swift returns 0.
    - grep -c "func refreshBridgeCatalogs() async" GooseSwift/HealthDataStore.swift returns 1.
    - The filtered audit (strip comment lines, then bridge.request without await) returns 0.
    - grep -c "DispatchWorkItem" GooseSwift/HealthDataStore.swift returns 0.
    - grep -c "packetInputRefreshTask: Task" GooseSwift/HealthDataStore.swift returns 1.
    - grep -c "func refreshSleepAfterBandSync(packetCount: Int) async" GooseSwift/HealthDataStore.swift returns 1.
    - grep -c "Task.sleep" GooseSwift/HealthDataStore.swift returns 1.
  </acceptance_criteria>
  <done>HealthDataStore.swift has no health GCD queues, no DispatchWorkItem, async catalog refresh, Task-based debounce, and a sequential async band-sync chain.</done>
</task>

<task type="auto">
  <name>Task 2: Wrap all external view/model callers in Task await</name>
  <files>GooseSwift/AppShellView.swift, GooseSwift/HealthView.swift, GooseSwift/HealthDashboardViews.swift, GooseSwift/HealthRecoveryStressViews.swift, GooseSwift/HealthSleepOverviewViews.swift, GooseSwift/HealthMetricFamilyStrainViews.swift, GooseSwift/SleepV2ScheduleViews.swift, GooseSwift/SleepBridgeViews.swift, GooseSwift/HomeDashboardView.swift, GooseSwift/MoreRawExportViews.swift, GooseSwift/MoreDataStore+Validation.swift, GooseSwift/CoachView.swift</files>
  <read_first>
    - .planning/phases/49-healthdatastore-async-migration/49-RESEARCH.md (the full "External Callers to Migrate (Views + Model)" table — exact file:line and method for each of the 14+ call sites)
    - Each listed view file at the cited line, to see the current sync call context (.onAppear / .onChange / Button action / closure)
  </read_first>
  <action>
    For each external call site of a now-async HealthDataStore method, wrap the call in a Task await closure (D-03, Pattern 3). Use the RESEARCH.md table as the authoritative checklist:

    - AppShellView.swift — onHistoricalSyncCompleted closure: wrap the healthStore.runPacketInputs() call in Task await.
    - HealthView.swift — refreshDashboard() (calls refreshBridgeCatalogs, refreshPacketInputsIfNeeded) and .onAppear (loadBridgeCatalogsIfNeeded): wrap each in Task await.
    - HealthDashboardViews.swift — Button actions calling runPacketInputs / runPacketScores: wrap each in Task await.
    - HealthRecoveryStressViews.swift — .onAppear/.onChange calling loadBridgeCatalogsIfNeeded + runPacketScores + runRecoveryV1 + runReadinessV1 + runV24Biometrics: wrap the group in a single Task await chain preserving order.
    - HealthSleepOverviewViews.swift — .onAppear calling loadBridgeCatalogsIfNeeded + runSleepStaging: wrap in one Task await.
    - HealthMetricFamilyStrainViews.swift — .onAppear/.onChange calling runExerciseSessions + runIMUStepCount: wrap in one Task await.
    - SleepV2ScheduleViews.swift and SleepBridgeViews.swift — Button actions calling refreshSleepAfterBandSync(packetCount:): wrap each in Task await.
    - HomeDashboardView.swift, MoreRawExportViews.swift, MoreDataStore+Validation.swift, CoachView.swift — .onAppear calling loadBridgeCatalogsIfNeeded (and refreshPacketInputsIfNeeded in CoachView): wrap each in Task await.

    Do not change call ordering inside each closure. Use weak store / weak self where the existing code already uses weak capture or where the closure outlives the view. Do not introduce any new sync store.run / store.refresh call.
  </action>
  <verify>
    <automated>grep -rn "store\.run\|store\.refresh\|store\.load\|healthStore\.run\|healthStore\.refresh\|healthStore\.load" GooseSwift/ --include="*.swift" | grep -v "await\|//\|func " | grep -vc "^$" || echo 0</automated>
  </verify>
  <acceptance_criteria>
    - The audit grep above (run/refresh call sites in the listed files NOT preceded by await on the same line, or not inside a Task await closure) returns 0 unwrapped sync calls.
    - grep -rn "Task" GooseSwift/HealthRecoveryStressViews.swift returns at least one Task wrapping the run-method group.
    - Every file in this task's files_modified that previously called a now-async method contains a Task await wrapper (verified by reading each cited call site).
  </acceptance_criteria>
  <done>All external callers of async HealthDataStore methods are wrapped in Task await; no unwrapped sync run/refresh call remains in the view layer.</done>
</task>

<task type="auto">
  <name>Task 3: Remove sync request/requestValue from GooseRustBridge; global grep audit</name>
  <files>GooseSwift/GooseRustBridge.swift</files>
  <read_first>
    - GooseSwift/GooseRustBridge.swift (sync request at line 26, sync requestValue at line 30, async requestAsync/requestValueAsync added in 49-01)
    - .planning/phases/49-healthdatastore-async-migration/49-CONTEXT.md (specifics: final cleanup removes sync request/requestValue)
  </read_first>
  <action>
    Run a repo-wide audit first: confirm there are zero remaining callers of the sync bridge methods anywhere under GooseSwift/. The audit must strip comment lines and exclude the requestAsync/requestValueAsync definitions and their internal use of requestValue. If the audit shows any non-test, non-async caller of sync request/requestValue, STOP and report which call site was missed (a wave-2 plan left a sync call) — do not delete the sync methods until the audit is clean.

    Once the audit is clean: the async requestValueAsync currently calls the sync requestValue internally (the FFI body). Keep the FFI body. The cleanest move: keep requestValue as the FFI executor but mark it private (so it is no longer a public entry point) while removing the public sync request wrapper (line 26). Concretely: delete the public sync request(method:args:) wrapper at line 26-28; mark requestValue as private (it is only called from requestValueAsync inside Task.detached). The public surface becomes requestAsync / requestValueAsync only. Update requestValueAsync to call the now-private requestValue.

    Remove the lastTiming nonisolated(unsafe) workaround only if lastTiming is unused after the audit (Open Question 2); otherwise leave it.
  </action>
  <verify>
    <automated>grep -rn "bridge.request(\|bridge.requestValue(" GooseSwift/ --include=*.swift | grep -v "//" | grep -vc await</automated>
  </verify>
  <acceptance_criteria>
    - The audit grep above returns 0 (no non-awaited bridge.request / bridge.requestValue caller remains anywhere in GooseSwift/).
    - grep -c "func request(method: String" GooseSwift/GooseRustBridge.swift returns 0 (public sync wrapper removed).
    - grep -c "private func requestValue(method: String" GooseSwift/GooseRustBridge.swift returns 1 (FFI executor now private) OR requestValue is otherwise non-public.
    - grep -c "func requestAsync" GooseSwift/GooseRustBridge.swift returns 1.
  </acceptance_criteria>
  <done>Public sync bridge API removed; requestAsync/requestValueAsync are the only public bridge entry points; global audit shows zero non-awaited bridge calls.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Build verification + dashboard smoke test</name>
  <files>GooseSwift/HealthDataStore.swift, GooseSwift/GooseRustBridge.swift</files>
  <action>
    This is a human-verification checkpoint. Claude first runs the automated build (XcodeBuildMCP build_sim, GooseSwift scheme, iPhone 16 simulator) and confirms BUILD SUCCEEDED with zero errors and zero new Swift Concurrency warnings (D-08a), then launches the app in the simulator and drives the UI (XcodeBuildMCP tap/swipe) to the Recovery V2, Sleep V2, and Esforco dashboards. The user then visually confirms each dashboard populates and that no background-publishing warning appears. See what-built / how-to-verify below.
  </action>
  <what-built>
    The full HealthDataStore async migration is complete: all 41 bridge call sites use await requestAsync, both GCD health queues are removed, the DispatchWorkItem debounce is now a Task, all external view callers are wrapped in Task await, and the public sync bridge API is removed.
  </what-built>
  <how-to-verify>
    1. Build the GooseSwift scheme for the iOS Simulator (XcodeBuildMCP build_sim, iPhone 16). Confirm "** BUILD SUCCEEDED **" with zero errors and zero new Swift Concurrency warnings (D-08a). Specifically confirm NO "Publishing changes from background threads" warning appears in the build/console output.
    2. Launch the app in the iOS Simulator. Drive the UI with XcodeBuildMCP (tap/swipe) — do not rely on build success alone.
    3. Navigate to the Recovery V2 dashboard — confirm it populates with recovery values (not "--"/empty) after the onAppear bridge call fires (D-08b).
    4. Navigate to the Sleep V2 dashboard — confirm sleep staging/score data populates.
    5. Navigate to the Esforco (Strain) dashboard — confirm strain/exercise data populates.
    6. Watch the Xcode console during navigation — confirm NO "Publishing changes from background threads is not allowed" message appears.
    7. Capture a screenshot of each of the three dashboards as evidence in the SUMMARY.
  </how-to-verify>
  <verify>
    <automated>xcodebuild build -scheme GooseSwift -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
    <human-check>Recovery V2, Sleep V2, and Esforco dashboards all populate in the simulator and no "Publishing changes from background threads" warning appears in the console.</human-check>
  </verify>
  <done>Build succeeds; all three dashboards populate; no background-publishing warning observed.</done>
  <resume-signal>Type "approved" if all three dashboards populate and no background-publishing warning appears; otherwise describe which dashboard failed or which warning appeared.</resume-signal>
</task>

</tasks>

<verification>
- HealthDataStore.swift: no health GCD queues, no DispatchWorkItem, async catalog refresh, Task debounce, async band-sync chain.
- All external view callers wrapped in Task await.
- GooseRustBridge: public sync API removed; only async entry points remain.
- Repo-wide grep: zero non-awaited bridge.request / bridge.requestValue.
- Build green; Recovery V2 / Sleep V2 / Esforco dashboards populate; no background-publishing warning.
</verification>

<success_criteria>
- ASYNC-01 closed: all 41 bridge calls migrated to await across 9 files; zero non-awaited bridge calls (grep audit 0).
- ASYNC-02 closed: dashboards populate post-migration; no main-thread freeze; no background-publishing warning.
- D-05 satisfied: both queues removed.
</success_criteria>

<output>
Create `.planning/phases/49-healthdatastore-async-migration/49-07-SUMMARY.md` when done. Include the three dashboard screenshots and the final grep-audit output (0 non-awaited bridge calls).
</output>
