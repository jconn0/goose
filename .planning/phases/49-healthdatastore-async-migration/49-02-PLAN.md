---
phase: 49-healthdatastore-async-migration
plan: 02
type: execute
wave: 2
depends_on: ["49-01"]
files_modified:
  - GooseSwift/HealthDataStore+PacketInputs.swift
  - GooseSwift/HealthDataStore.swift
autonomous: true
requirements: [ASYNC-01, ASYNC-02]
must_haves:
  truths:
    - "packetInputBridgeReports runs its 21 bridge calls via await requestAsync, off @MainActor"
    - "runPacketInputs is async and awaits packetInputBridgeReports without packetInputQueue dispatch"
  artifacts:
    - path: "GooseSwift/HealthDataStore+PacketInputs.swift"
      provides: "nonisolated static async packetInputBridgeReports with 21 awaited bridge calls"
      contains: "async"
    - path: "GooseSwift/HealthDataStore.swift"
      provides: "async runPacketInputs that awaits packetInputBridgeReports"
      contains: "func runPacketInputs"
  key_links:
    - from: "HealthDataStore.runPacketInputs"
      to: "HealthDataStore.packetInputBridgeReports"
      via: "await static call"
      pattern: "await .*packetInputBridgeReports"
    - from: "packetInputBridgeReports"
      to: "bridge.requestAsync"
      via: "await on its own GooseRustBridge instance"
      pattern: "await .*requestAsync"
---

<objective>
Migrate the largest single batch of bridge calls: `packetInputBridgeReports` in `HealthDataStore+PacketInputs.swift` (21 sequential bridge calls inside one `nonisolated static` function with its own `GooseRustBridge()` instance) becomes `nonisolated static async`, awaiting `bridge.requestAsync` for every call (D-04, D-07). The caller `runPacketInputs` in `HealthDataStore.swift` becomes `async func` and awaits `packetInputBridgeReports` directly, removing its `packetInputQueue.async` wrapper.

Purpose: Convert the bulk of the call sites in one cohesive plan. `packetInputQueue` and `heartRateTimelineQueue` are NOT removed here — removal is deferred to the final cleanup plan (49-07) so the rest of the not-yet-migrated files still build (D-06).

Note on build gate: Wave-2 plans (49-02 through 49-06) convert store methods to `async`. Their external SwiftUI/GooseAppModel callers are migrated together in 49-07 (those view files are shared across many run* methods, so editing them in one plan avoids inter-plan file overlap per D-06). Consequently, wave-2 plans verify correctness with grep/source assertions and a per-file compile check; the full clean `xcodebuild build` gate (D-08a) is asserted in 49-07 after all callers are wrapped. To keep this plan internally consistent, where a method in this plan's own files calls a now-async method, wrap it in `Task { await ... }` or chain the await.

Output: async `packetInputBridgeReports` + async `runPacketInputs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-healthdatastore-async-migration/49-CONTEXT.md
@.planning/phases/49-healthdatastore-async-migration/49-RESEARCH.md
@.planning/phases/49-healthdatastore-async-migration/49-01-SUMMARY.md
@GooseSwift/GooseRustBridge.swift
@GooseSwift/HealthDataStore+PacketInputs.swift
@GooseSwift/HealthDataStore.swift
</context>

## Artifacts this plan produces

- `HealthDataStore.packetInputBridgeReports(databasePath:) async -> Result<...>` — converted to `nonisolated static async` (21 awaited bridge calls)
- `HealthDataStore.runPacketInputs() async` — converted to async func; `completion:` closure parameter removed in favour of sequential await

<tasks>

<task type="auto">
  <name>Task 1: Convert packetInputBridgeReports to nonisolated static async</name>
  <files>GooseSwift/HealthDataStore+PacketInputs.swift</files>
  <read_first>
    - GooseSwift/HealthDataStore+PacketInputs.swift (whole file — `nonisolated static func packetInputBridgeReports`, its 21 `bridge.request(...)` calls, its own `let bridge = GooseRustBridge()` instance)
    - GooseSwift/GooseRustBridge.swift (new `requestAsync` from 49-01)
    - .planning/phases/49-healthdatastore-async-migration/49-RESEARCH.md (Pattern 4 — Static nonisolated async)
  </read_first>
  <action>
    Change the signature of `packetInputBridgeReports(databasePath:)` from `nonisolated static func ... -> Result<[String: [String: Any]], Error>` to `nonisolated static func ... async -> Result<[String: [String: Any]], Error>` (add `async` before the return arrow; keep `nonisolated static`).

    Replace every one of the 21 `try bridge.request(method: ..., args: ...)` calls inside this function with `try await bridge.requestAsync(method: ..., args: ...)`. Keep the function's own local `let bridge = GooseRustBridge()` instance unchanged. The 21 calls remain sequential, each awaiting. Preserve the existing `do/catch` → `.success`/`.failure` Result wrapping exactly; only `bridge.request` becomes `await bridge.requestAsync`.

    Do not touch any non-bridge logic (args construction, report dictionary assembly). Do not add `packetInputQueue` usage. After this change, zero non-awaited `bridge.request(` must remain in this file.
  </action>
  <verify>
    <automated>grep -v '^[[:space:]]*//' GooseSwift/HealthDataStore+PacketInputs.swift | grep -c "await bridge.requestAsync"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^[[:space:]]*//' GooseSwift/HealthDataStore+PacketInputs.swift | grep -c "await bridge.requestAsync"` returns 21.
    - `grep -v '^[[:space:]]*//' GooseSwift/HealthDataStore+PacketInputs.swift | grep "bridge.request(" | grep -vc "await"` returns 0.
    - `grep -c "nonisolated static func packetInputBridgeReports" GooseSwift/HealthDataStore+PacketInputs.swift` returns 1 and the signature contains `async`.
  </acceptance_criteria>
  <done>All 21 calls awaited; function is `nonisolated static async`; no sync bridge call remains in the file.</done>
</task>

<task type="auto">
  <name>Task 2: Convert runPacketInputs to async, awaiting packetInputBridgeReports</name>
  <files>GooseSwift/HealthDataStore.swift</files>
  <read_first>
    - GooseSwift/HealthDataStore.swift (the `runPacketInputs(completion:)` method ~line 263-277 dispatching to `packetInputQueue` and calling `packetInputBridgeReports`; `packetInputQueue` line 53 and `heartRateTimelineQueue` line 54 — DO NOT remove them in this plan)
    - GooseSwift/HealthDataStore+PacketInputs.swift (the now-async `packetInputBridgeReports` from Task 1)
    - .planning/phases/49-healthdatastore-async-migration/49-RESEARCH.md (Pattern 4 caller migration; RISK-03 completion-closure note)
  </read_first>
  <action>
    Convert `runPacketInputs` from sync (`func runPacketInputs(completion: (() -> Void)? = nil)` dispatching to `packetInputQueue.async`) to `func runPacketInputs() async`.

    New body: capture `databasePath` (and any other @MainActor state read inside) into local `let` constants before any await; then `let result = await HealthDataStore.packetInputBridgeReports(databasePath: databasePath)`; then `switch result` mutating @Observable properties directly on @MainActor after the await (`self.packetInputReports = reports` and the status strings on `.success`; the blocked-status string on `.failure`). Remove the `packetInputQueue.async { }` wrapper and any inner `DispatchQueue.main.async { }`. Remove the `completion: (() -> Void)? = nil` parameter.

    In-file callers of `runPacketInputs` that live in `HealthDataStore.swift` (e.g. `refreshPacketInputsAfterCapture`, `refreshPacketInputsIfNeeded`, `runPacketInputs(completion:)` users): make each calling method `async` and chain `await self.runPacketInputs()`, OR if the caller must stay sync (e.g. it is invoked from a sync debounce), wrap with `Task { await self.runPacketInputs() }`. For `refreshPacketInputsAfterCapture` (uses `DispatchWorkItem`), leave its debounce mechanism intact for now and only swap the inner `runPacketInputs()` call to `Task { await self.runPacketInputs() }` — full DispatchWorkItem→Task migration is owned by 49-07 (RISK-04). Do NOT remove `packetInputQueue` or `heartRateTimelineQueue` (deferred to 49-07).

    Do NOT edit external view/model files (AppShellView, HealthDashboardViews) — their `runPacketInputs()` call sites are wrapped in `Task { await ... }` in 49-07.
  </action>
  <verify>
    <automated>grep -c "func runPacketInputs() async" GooseSwift/HealthDataStore.swift</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "func runPacketInputs() async" GooseSwift/HealthDataStore.swift` returns 1.
    - `grep "await .*packetInputBridgeReports" GooseSwift/HealthDataStore.swift` returns at least one match.
    - `grep -c "func runPacketInputs(completion" GooseSwift/HealthDataStore.swift` returns 0 (completion-parameter overload removed).
    - `grep -c "let packetInputQueue" GooseSwift/HealthDataStore.swift` returns 1 (queue declaration retained — deferred removal).
    - Every in-file caller of `runPacketInputs` either is `async` and uses `await`, or wraps the call in `Task { await ... }` (verified by reading the file — no bare sync `self.runPacketInputs()` call remains: `grep "runPacketInputs()" GooseSwift/HealthDataStore.swift | grep -v "await\|func "` returns 0 lines).
  </acceptance_criteria>
  <done>runPacketInputs is async, awaits the async packetInputBridgeReports, no longer dispatches via packetInputQueue; queues retained for 49-07; external callers untouched.</done>
</task>

</tasks>

<verification>
- 21 bridge calls in +PacketInputs awaited via requestAsync.
- runPacketInputs async; packetInputBridgeReports async.
- packetInputQueue/heartRateTimelineQueue declarations retained for later files.
</verification>

<success_criteria>
- Largest call-site batch (21+1) migrated to async (ASYNC-01 progress).
- @Observable mutations happen on @MainActor after await (ASYNC-02 preserved).
</success_criteria>

<output>
Create `.planning/phases/49-healthdatastore-async-migration/49-02-SUMMARY.md` when done. List any in-file `Task { await }` shims added so 49-07 can finalize.
</output>
