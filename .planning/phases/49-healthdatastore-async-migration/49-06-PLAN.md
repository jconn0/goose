---
phase: 49-healthdatastore-async-migration
plan: 06
type: execute
wave: 2
depends_on: ["49-01"]
files_modified:
  - GooseSwift/HealthDataStore+Cardio.swift
  - GooseSwift/HealthDataStore+Utilities.swift
autonomous: true
requirements: [ASYNC-01, ASYNC-02]
must_haves:
  truths:
    - "cardioLoadActivitySessions and cardioLoadActivityMetricsByName no longer call bridge.request synchronously on @MainActor"
    - "sleepScoreReport helper no longer calls bridge.request synchronously"
    - "No non-awaited bridge.request remains in either file"
  artifacts:
    - path: "GooseSwift/HealthDataStore+Cardio.swift"
      provides: "async cardioLoadActivitySessions / cardioLoadActivityMetricsByName using await requestAsync"
      contains: "async"
    - path: "GooseSwift/HealthDataStore+Utilities.swift"
      provides: "async sleepScoreReport helper using await requestAsync"
      contains: "async"
  key_links:
    - from: "cardioLoadActivitySessions / cardioLoadActivityMetricsByName / sleepScoreReport"
      to: "bridge.requestAsync"
      via: "await after capturing @MainActor state"
      pattern: "await .*requestAsync"
---

<objective>
Resolve the highest-risk pattern: `HealthDataStore+Cardio.swift` has two bridge calls (`cardioLoadActivitySessions` at line 97, `cardioLoadActivityMetricsByName` at line 116) that run **directly and synchronously on @MainActor** with no queue dispatch (RISK-02, Pattern 5) — the only place in the codebase where a bridge FFI blocks the main thread. Migrate both to `async` using `await bridge.requestAsync` (D-04). Also migrate the `sleepScoreReport` helper in `HealthDataStore+Utilities.swift` (line 125), which calls `bridge.request` directly.

Investigation outcome (resolves Open Question 1 / A1 / A2 from RESEARCH.md — verified by grep): `cardioLoadActivitySessions`, `cardioLoadActivityMetricsByName`, `cardioLoadContribution`, `cardioLoadDailyComputations`, and `sleepScoreReport` have ZERO live callers. `cardioLoadAlgorithmSummary` (the only function reachable from SwiftUI body via `cardioStatusRows`/`cardioLoadSnapshot`) currently returns an empty stub summary and does NOT call the two bridge methods. Therefore making them `async` has no cascade into a SwiftUI body — the redesign feared in RISK-02 is unnecessary. The methods are dead/future code; converting their signatures to `async` keeps them compiling without touching any caller.

Purpose: Eliminate the one actual main-thread FFI block and remove the last direct sync bridge calls. Wave-2 parallel batch, zero file overlap with 49-02/03/04/05.
Output: async `cardioLoadActivitySessions`, `cardioLoadActivityMetricsByName`, `sleepScoreReport`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-healthdatastore-async-migration/49-CONTEXT.md
@.planning/phases/49-healthdatastore-async-migration/49-RESEARCH.md
@.planning/phases/49-healthdatastore-async-migration/49-01-SUMMARY.md
@GooseSwift/GooseRustBridge.swift
@GooseSwift/HealthDataStore+Cardio.swift
@GooseSwift/HealthDataStore+Utilities.swift
</context>

## Artifacts this plan produces

- `HealthDataStore.cardioLoadActivitySessions(from:to:) async -> [[String: Any]]`
- `HealthDataStore.cardioLoadActivityMetricsByName(sessionID:) async -> [String: [String: Any]]`
- `HealthDataStore.sleepScoreReport(baseArgs:) async throws -> [String: Any]`

<tasks>

<task type="auto">
  <name>Task 1: Convert the two direct Cardio bridge calls to async</name>
  <files>GooseSwift/HealthDataStore+Cardio.swift</files>
  <read_first>
    - GooseSwift/HealthDataStore+Cardio.swift (whole file — `cardioLoadActivitySessions` lines 95-109 with direct `try bridge.request` at line 97; `cardioLoadActivityMetricsByName` lines 111-135 with direct `try bridge.request` at line 116; note `cardioLoadAlgorithmSummary` lines 15-30 returns empty stub and does NOT call these two methods)
    - GooseSwift/GooseRustBridge.swift (requestAsync from 49-01)
    - .planning/phases/49-healthdatastore-async-migration/49-RESEARCH.md (Pattern 5 — Cardio direct bridge calls)
  </read_first>
  <action>
    Change `cardioLoadActivitySessions(from:to:)` signature to `func cardioLoadActivitySessions(from start: Date, to end: Date) async -> [[String: Any]]` (insert `async` before the return arrow). Capture `databasePath` into a local constant before the await. Replace `try bridge.request(method: "activity.list_sessions", args: [...])` with `try await bridge.requestAsync(method: "activity.list_sessions", args: [...])`. Keep the `do/catch` returning `report["sessions"] as? [[String: Any]] ?? []` on success and `[]` on error.

    Change `cardioLoadActivityMetricsByName(sessionID:)` signature to `func cardioLoadActivityMetricsByName(sessionID: String?) async -> [String: [String: Any]]`. Keep the `guard let sessionID else { return [:] }`. Capture `databasePath` before the await. Replace `try bridge.request(method: "activity.list_metrics", args: [...])` with `try await bridge.requestAsync(...)`. Keep the existing metrics dictionary-building logic and `[:]` error fallback unchanged.

    Do NOT touch `cardioLoadAlgorithmSummary`, `cardioStatusRows`, `cardioLoadSnapshot`, `cardioLoadContribution`, or `cardioLoadDailyComputations` — they do not call the bridge. No `bridge.request` (non-awaited) may remain in the file.
  </action>
  <verify>
    <automated>grep -v '^[[:space:]]*//' GooseSwift/HealthDataStore+Cardio.swift | grep -c "await bridge.requestAsync"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^[[:space:]]*//' GooseSwift/HealthDataStore+Cardio.swift | grep -c "await bridge.requestAsync"` returns 2.
    - `grep -v '^[[:space:]]*//' GooseSwift/HealthDataStore+Cardio.swift | grep "bridge.request(" | grep -vc "await"` returns 0.
    - `grep -c "func cardioLoadActivitySessions(from start: Date, to end: Date) async" GooseSwift/HealthDataStore+Cardio.swift` returns 1.
    - `grep -c "func cardioLoadActivityMetricsByName(sessionID: String?) async" GooseSwift/HealthDataStore+Cardio.swift` returns 1.
  </acceptance_criteria>
  <done>Both direct Cardio bridge methods are async and awaited; the empty-stub summary path is untouched; no sync bridge call remains.</done>
</task>

<task type="auto">
  <name>Task 2: Convert sleepScoreReport helper to async (Utilities)</name>
  <files>GooseSwift/HealthDataStore+Utilities.swift</files>
  <read_first>
    - GooseSwift/HealthDataStore+Utilities.swift (whole file — `sleepScoreReport(baseArgs:) throws -> [String: Any]` at line 124 with direct `try bridge.request(...)` at line 125)
    - GooseSwift/GooseRustBridge.swift (requestAsync from 49-01)
    - .planning/phases/49-healthdatastore-async-migration/49-RESEARCH.md (Utilities note — sleepScoreReport has no callers)
  </read_first>
  <action>
    Change `sleepScoreReport(baseArgs:)` signature to `func sleepScoreReport(baseArgs: [String: Any]) async throws -> [String: Any]` (insert `async` before `throws`). Replace the direct `try bridge.request(...)` with `try await bridge.requestAsync(...)`. Preserve the args and return shape exactly. (This helper has zero callers per grep — it is migrated for D-04 completeness so no non-awaited bridge call remains anywhere; 49-07's final grep audit will confirm zero sync `bridge.request` callers exist before the sync method is removed.) Do not delete the function.
  </action>
  <verify>
    <automated>grep -v '^[[:space:]]*//' GooseSwift/HealthDataStore+Utilities.swift | grep -c "await bridge.requestAsync"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^[[:space:]]*//' GooseSwift/HealthDataStore+Utilities.swift | grep -c "await bridge.requestAsync"` returns 1.
    - `grep -v '^[[:space:]]*//' GooseSwift/HealthDataStore+Utilities.swift | grep "bridge.request(" | grep -vc "await"` returns 0.
    - `grep -c "func sleepScoreReport(baseArgs: \[String: Any\]) async throws" GooseSwift/HealthDataStore+Utilities.swift` returns 1.
  </acceptance_criteria>
  <done>sleepScoreReport is async throws and awaits requestAsync; no sync bridge call remains in the file.</done>
</task>

</tasks>

<verification>
- 3 bridge calls across the two files awaited via requestAsync.
- The only direct-on-@MainActor FFI block is eliminated.
- No non-awaited bridge.request remains in either file.
</verification>

<success_criteria>
- Cardio direct calls + Utilities helper migrated (ASYNC-01 — completes per-file migration); the actual main-thread block from Pattern 5 is removed (ASYNC-02).
</success_criteria>

<output>
Create `.planning/phases/49-healthdatastore-async-migration/49-06-SUMMARY.md` when done. Confirm the dead-code finding (no callers) held during implementation.
</output>
