---
phase: 49-healthdatastore-async-migration
plan: 05
type: execute
wave: 2
depends_on: ["49-01"]
files_modified:
  - GooseSwift/HealthDataStore+Exercise.swift
  - GooseSwift/HealthDataStore+IMUSteps.swift
  - GooseSwift/HealthDataStore+V24Biometrics.swift
autonomous: true
requirements: [ASYNC-01, ASYNC-02]
must_haves:
  truths:
    - "runExerciseSessions, runIMUStepCount, runV24Biometrics are async and await bridge.requestAsync"
    - "The try? secondary spo2 call in V24Biometrics becomes try? await requestAsync"
    - "No packetInputQueue.async wrapper remains in any of the three files"
  artifacts:
    - path: "GooseSwift/HealthDataStore+Exercise.swift"
      provides: "async runExerciseSessions (1 awaited bridge call)"
      contains: "async"
    - path: "GooseSwift/HealthDataStore+IMUSteps.swift"
      provides: "async runIMUStepCount (2 awaited sequential bridge calls)"
      contains: "async"
    - path: "GooseSwift/HealthDataStore+V24Biometrics.swift"
      provides: "async runV24Biometrics (2 awaited bridge calls incl. try?)"
      contains: "async"
  key_links:
    - from: "runExerciseSessions / runIMUStepCount / runV24Biometrics"
      to: "bridge.requestAsync"
      via: "await after capturing @MainActor state"
      pattern: "await .*requestAsync"
---

<objective>
Migrate the activity/IMU/biometrics runners from `packetInputQueue.async { ... Task { @MainActor [weak self] in } }` to `async func` using `await bridge.requestAsync` (D-04, D-07):
- `HealthDataStore+Exercise.swift` — `runExerciseSessions` (1 call)
- `HealthDataStore+IMUSteps.swift` — `runIMUStepCount` (2 sequential calls)
- `HealthDataStore+V24Biometrics.swift` — `runV24Biometrics` (2 calls, second is `try?` optional spo2)

Purpose: Wave-2 parallel batch. Zero file overlap with 49-02/03/04/06. External view callers (HealthMetricFamilyStrainViews) are wrapped in 49-07.
Output: async `runExerciseSessions`, `runIMUStepCount`, `runV24Biometrics`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-healthdatastore-async-migration/49-CONTEXT.md
@.planning/phases/49-healthdatastore-async-migration/49-RESEARCH.md
@.planning/phases/49-healthdatastore-async-migration/49-01-SUMMARY.md
@GooseSwift/GooseRustBridge.swift
@GooseSwift/HealthDataStore+Exercise.swift
@GooseSwift/HealthDataStore+IMUSteps.swift
@GooseSwift/HealthDataStore+V24Biometrics.swift
</context>

## Artifacts this plan produces

- `HealthDataStore.runExerciseSessions() async`
- `HealthDataStore.runIMUStepCount() async`
- `HealthDataStore.runV24Biometrics() async`

<tasks>

<task type="auto">
  <name>Task 1: Migrate runExerciseSessions to async (Exercise)</name>
  <files>GooseSwift/HealthDataStore+Exercise.swift</files>
  <read_first>
    - GooseSwift/HealthDataStore+Exercise.swift (whole file — `runExerciseSessions` with bridge call at ~line 91; `packetInputQueue.async { ... Task { @MainActor [weak self] in } }` pattern)
    - GooseSwift/GooseRustBridge.swift (requestAsync from 49-01)
    - .planning/phases/49-healthdatastore-async-migration/49-RESEARCH.md (Pattern 2)
  </read_first>
  <action>
    Convert `runExerciseSessions` to `func runExerciseSessions() async`. Remove the `packetInputQueue.async { [weak self] in ... }` wrapper and the inner `Task { @MainActor [weak self] in ... }`. Capture @MainActor inputs into local constants before the await. Replace `try bridge.request(...)` with `try await bridge.requestAsync(...)`. After the await, mutate the exercise-sessions @Observable result on `self`. Preserve existing error handling and parsing. No `packetInputQueue` reference may remain.
  </action>
  <verify>
    <automated>grep -v '^[[:space:]]*//' GooseSwift/HealthDataStore+Exercise.swift | grep -c "await bridge.requestAsync"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^[[:space:]]*//' GooseSwift/HealthDataStore+Exercise.swift | grep -c "await bridge.requestAsync"` returns 1.
    - `grep -v '^[[:space:]]*//' GooseSwift/HealthDataStore+Exercise.swift | grep "bridge.request(" | grep -vc "await"` returns 0.
    - `grep -c "func runExerciseSessions() async" GooseSwift/HealthDataStore+Exercise.swift` returns 1.
    - `grep -c "packetInputQueue" GooseSwift/HealthDataStore+Exercise.swift` returns 0.
  </acceptance_criteria>
  <done>runExerciseSessions async, call awaited, no queue dispatch remains.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate runIMUStepCount to async with 2 sequential awaits (IMUSteps)</name>
  <files>GooseSwift/HealthDataStore+IMUSteps.swift</files>
  <read_first>
    - GooseSwift/HealthDataStore+IMUSteps.swift (whole file — `runIMUStepCount` with 2 bridge calls at ~lines 50 and 78 within one dispatch; first fetches gravity rows, second runs step counting on the result)
    - GooseSwift/GooseRustBridge.swift (requestAsync from 49-01)
    - .planning/phases/49-healthdatastore-async-migration/49-RESEARCH.md (IMUSteps 2-call note)
  </read_first>
  <action>
    Convert `runIMUStepCount` to `func runIMUStepCount() async`. Remove the `packetInputQueue.async { [weak self] in ... }` wrapper and the inner `Task { @MainActor [weak self] in ... }`. Capture @MainActor inputs before the first await. Replace BOTH `try bridge.request(...)` calls with `try await bridge.requestAsync(...)`, preserving order: first await fetches gravity rows, then second await runs step counting using the first result. Keep the data flow between the two calls identical. After the awaits, mutate the step-count @Observable result on `self`. Preserve existing error handling. No `packetInputQueue` reference may remain.
  </action>
  <verify>
    <automated>grep -v '^[[:space:]]*//' GooseSwift/HealthDataStore+IMUSteps.swift | grep -c "await bridge.requestAsync"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^[[:space:]]*//' GooseSwift/HealthDataStore+IMUSteps.swift | grep -c "await bridge.requestAsync"` returns 2.
    - `grep -v '^[[:space:]]*//' GooseSwift/HealthDataStore+IMUSteps.swift | grep "bridge.request(" | grep -vc "await"` returns 0.
    - `grep -c "func runIMUStepCount() async" GooseSwift/HealthDataStore+IMUSteps.swift` returns 1.
    - `grep -c "packetInputQueue" GooseSwift/HealthDataStore+IMUSteps.swift` returns 0.
  </acceptance_criteria>
  <done>runIMUStepCount async, both calls awaited in order, no queue dispatch remains.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate runV24Biometrics to async incl. optional try? spo2 (V24Biometrics)</name>
  <files>GooseSwift/HealthDataStore+V24Biometrics.swift</files>
  <read_first>
    - GooseSwift/HealthDataStore+V24Biometrics.swift (whole file — `runV24Biometrics` with primary bridge call at ~line 64 and conditional secondary `try? bridge.request(...)` spo2 call at ~line 93, both inside one dispatch)
    - GooseSwift/GooseRustBridge.swift (requestAsync from 49-01)
    - .planning/phases/49-healthdatastore-async-migration/49-RESEARCH.md (Pitfall 4 — try? bridge call in V24Biometrics)
  </read_first>
  <action>
    Convert `runV24Biometrics` to `func runV24Biometrics() async`. Remove the `packetInputQueue.async { [weak self] in ... }` wrapper and the inner `Task { @MainActor [weak self] in ... }`. Capture @MainActor inputs before the first await. Replace the primary `try bridge.request(...)` with `try await bridge.requestAsync(...)`. Replace the secondary conditional `if let spo2Report = try? bridge.request(...)` with `if let spo2Report = try? await bridge.requestAsync(...)` — preserve the `try?` optional semantics exactly (Pitfall 4). After the awaits, mutate the V24 biometric @Observable results on `self`. Preserve the existing `quality_flag` handling and all parsing. No `packetInputQueue` reference may remain.
  </action>
  <verify>
    <automated>grep -v '^[[:space:]]*//' GooseSwift/HealthDataStore+V24Biometrics.swift | grep -c "await bridge.requestAsync"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^[[:space:]]*//' GooseSwift/HealthDataStore+V24Biometrics.swift | grep -c "await bridge.requestAsync"` returns 2.
    - `grep -v '^[[:space:]]*//' GooseSwift/HealthDataStore+V24Biometrics.swift | grep "bridge.request(" | grep -vc "await"` returns 0.
    - `grep -c "try? await bridge.requestAsync" GooseSwift/HealthDataStore+V24Biometrics.swift` returns 1 (optional spo2 call preserves try? semantics).
    - `grep -c "func runV24Biometrics() async" GooseSwift/HealthDataStore+V24Biometrics.swift` returns 1.
    - `grep -c "packetInputQueue" GooseSwift/HealthDataStore+V24Biometrics.swift` returns 0.
  </acceptance_criteria>
  <done>runV24Biometrics async, both calls awaited (primary throws, secondary try?), no queue dispatch remains.</done>
</task>

</tasks>

<verification>
- 5 bridge calls across the three files awaited via requestAsync.
- Three run* methods async; no packetInputQueue usage in any file.
- try? semantics preserved for optional spo2 call.
</verification>

<success_criteria>
- Exercise + IMU + V24 migrated (ASYNC-01 progress); @MainActor mutations preserved (ASYNC-02).
</success_criteria>

<output>
Create `.planning/phases/49-healthdatastore-async-migration/49-05-SUMMARY.md` when done.
</output>
