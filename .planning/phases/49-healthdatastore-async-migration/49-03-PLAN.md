---
phase: 49-healthdatastore-async-migration
plan: 03
type: execute
wave: 2
depends_on: ["49-01"]
files_modified:
  - GooseSwift/HealthDataStore+Snapshots.swift
  - GooseSwift/HealthDataStore+Recovery.swift
autonomous: true
requirements: [ASYNC-01, ASYNC-02]
must_haves:
  truths:
    - "runPacketScores, runSleepScore, runRecoveryV1 are async and await bridge.requestAsync"
    - "No packetInputQueue.async wrapper remains in either file"
  artifacts:
    - path: "GooseSwift/HealthDataStore+Snapshots.swift"
      provides: "async runPacketScores and runSleepScore (5 awaited bridge calls)"
      contains: "async"
    - path: "GooseSwift/HealthDataStore+Recovery.swift"
      provides: "async runRecoveryV1 (1 awaited bridge call)"
      contains: "async"
  key_links:
    - from: "runPacketScores / runSleepScore / runRecoveryV1"
      to: "bridge.requestAsync"
      via: "await after capturing @MainActor state"
      pattern: "await .*requestAsync"
---

<objective>
Migrate the score-runner methods in `HealthDataStore+Snapshots.swift` (`runPacketScores`, `runSleepScore` — 5 bridge calls) and `HealthDataStore+Recovery.swift` (`runRecoveryV1` — 1 bridge call) from `packetInputQueue.async { ... DispatchQueue.main.async { } }` to `async func` using `await bridge.requestAsync` (D-04, D-07). @Observable property mutations happen directly on @MainActor after the await (D-02).

Purpose: Wave-2 parallel batch. Zero file overlap with 49-02/04/05/06. External view callers (HealthRecoveryStressViews, HealthDashboardViews) are wrapped in 49-07.
Output: async `runPacketScores`, `runSleepScore`, `runRecoveryV1`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-healthdatastore-async-migration/49-CONTEXT.md
@.planning/phases/49-healthdatastore-async-migration/49-RESEARCH.md
@.planning/phases/49-healthdatastore-async-migration/49-01-SUMMARY.md
@GooseSwift/GooseRustBridge.swift
@GooseSwift/HealthDataStore+Snapshots.swift
@GooseSwift/HealthDataStore+Recovery.swift
</context>

## Artifacts this plan produces

- `HealthDataStore.runPacketScores() async`
- `HealthDataStore.runSleepScore() async`
- `HealthDataStore.runRecoveryV1() async`

<tasks>

<task type="auto">
  <name>Task 1: Migrate runPacketScores and runSleepScore to async (Snapshots)</name>
  <files>GooseSwift/HealthDataStore+Snapshots.swift</files>
  <read_first>
    - GooseSwift/HealthDataStore+Snapshots.swift (whole file — `runPacketScores` with 4 bridge calls at ~lines 31,42,46,50; `runSleepScore` with 1 bridge call at ~line 80; current `packetInputQueue.async { ... DispatchQueue.main.async { } }` pattern)
    - GooseSwift/GooseRustBridge.swift (requestAsync from 49-01)
    - .planning/phases/49-healthdatastore-async-migration/49-RESEARCH.md (Pattern 2 + canonical async migration example)
  </read_first>
  <action>
    Convert `runPacketScores` and `runSleepScore` to `func ...() async`.

    For each: remove the `packetInputQueue.async { [weak self] in ... }` wrapper and any inner `DispatchQueue.main.async { self.x = r }`. Capture all @MainActor state (e.g. `databasePath`, args inputs) into local `let` constants BEFORE the first await. Replace each `try bridge.request(method:args:)` with `try await bridge.requestAsync(method:args:)`. After the await, mutate the @Observable result properties directly on `self` (back on @MainActor). Preserve all existing `do/catch` error handling and the exact result-parsing logic; only the dispatch wrapper and `bridge.request` change.

    `runPacketScores` has 4 sequential bridge calls — keep them sequential, each awaited, in the same order. `runSleepScore` has 1. No `packetInputQueue` reference may remain in either method.
  </action>
  <verify>
    <automated>grep -v '^[[:space:]]*//' GooseSwift/HealthDataStore+Snapshots.swift | grep -c "await bridge.requestAsync"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^[[:space:]]*//' GooseSwift/HealthDataStore+Snapshots.swift | grep -c "await bridge.requestAsync"` returns 5.
    - `grep -v '^[[:space:]]*//' GooseSwift/HealthDataStore+Snapshots.swift | grep "bridge.request(" | grep -vc "await"` returns 0.
    - `grep -c "func runPacketScores() async" GooseSwift/HealthDataStore+Snapshots.swift` returns 1.
    - `grep -c "func runSleepScore() async" GooseSwift/HealthDataStore+Snapshots.swift` returns 1.
    - `grep -c "packetInputQueue" GooseSwift/HealthDataStore+Snapshots.swift` returns 0.
  </acceptance_criteria>
  <done>Both methods async, 5 calls awaited, no queue dispatch remains.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate runRecoveryV1 to async (Recovery)</name>
  <files>GooseSwift/HealthDataStore+Recovery.swift</files>
  <read_first>
    - GooseSwift/HealthDataStore+Recovery.swift (whole file — `runRecoveryV1` with bridge call at ~line 115; current `packetInputQueue.async { ... Task { @MainActor [weak self] in } }` pattern at ~line 132)
    - GooseSwift/GooseRustBridge.swift (requestAsync from 49-01)
    - .planning/phases/49-healthdatastore-async-migration/49-RESEARCH.md (canonical async migration of a run* method — exact BEFORE/AFTER for runRecoveryV1)
  </read_first>
  <action>
    Convert `runRecoveryV1` to `func runRecoveryV1() async`. Remove the `packetInputQueue.async { [weak self] in ... }` wrapper and the inner `Task { @MainActor [weak self] in self?.recoveryV1Result = result }`. Capture `databasePath` and the recovery `bridgeArgs` inputs from @MainActor state into local constants before the await. Replace `try bridge.request(method: "metrics.goose_recovery_v1", args: bridgeArgs)` with `try await bridge.requestAsync(method: "metrics.goose_recovery_v1", args: bridgeArgs)`. After the await, set `self.recoveryV1Result = result` (success) directly; on `catch`, set `self.recoveryV1Result = nil`. No `packetInputQueue` reference may remain.
  </action>
  <verify>
    <automated>grep -v '^[[:space:]]*//' GooseSwift/HealthDataStore+Recovery.swift | grep -c "await bridge.requestAsync"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^[[:space:]]*//' GooseSwift/HealthDataStore+Recovery.swift | grep -c "await bridge.requestAsync"` returns 1.
    - `grep -v '^[[:space:]]*//' GooseSwift/HealthDataStore+Recovery.swift | grep "bridge.request(" | grep -vc "await"` returns 0.
    - `grep -c "func runRecoveryV1() async" GooseSwift/HealthDataStore+Recovery.swift` returns 1.
    - `grep -c "packetInputQueue" GooseSwift/HealthDataStore+Recovery.swift` returns 0.
  </acceptance_criteria>
  <done>runRecoveryV1 async, call awaited, no queue dispatch remains.</done>
</task>

</tasks>

<verification>
- 6 bridge calls across the two files awaited via requestAsync.
- Three run* methods are async; no packetInputQueue usage in either file.
</verification>

<success_criteria>
- Snapshots + Recovery migrated (ASYNC-01 progress); @MainActor mutations preserved (ASYNC-02).
</success_criteria>

<output>
Create `.planning/phases/49-healthdatastore-async-migration/49-03-SUMMARY.md` when done.
</output>
