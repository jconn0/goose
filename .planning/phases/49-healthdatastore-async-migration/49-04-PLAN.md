---
phase: 49-healthdatastore-async-migration
plan: 04
type: execute
wave: 2
depends_on: ["49-01"]
files_modified:
  - GooseSwift/HealthDataStore+StagingSleep.swift
  - GooseSwift/HealthDataStore+Readiness.swift
autonomous: true
requirements: [ASYNC-01, ASYNC-02]
must_haves:
  truths:
    - "runSleepStaging and runReadinessV1 are async and await bridge.requestAsync"
    - "No packetInputQueue.async wrapper remains in either file"
  artifacts:
    - path: "GooseSwift/HealthDataStore+StagingSleep.swift"
      provides: "async runSleepStaging (1 awaited bridge call)"
      contains: "async"
    - path: "GooseSwift/HealthDataStore+Readiness.swift"
      provides: "async runReadinessV1 (2 awaited sequential bridge calls)"
      contains: "async"
  key_links:
    - from: "runSleepStaging / runReadinessV1"
      to: "bridge.requestAsync"
      via: "await after capturing @MainActor state"
      pattern: "await .*requestAsync"
---

<objective>
Migrate `HealthDataStore+StagingSleep.swift` (`runSleepStaging` — 1 bridge call) and `HealthDataStore+Readiness.swift` (`runReadinessV1` — 2 sequential bridge calls within one dispatch) from `packetInputQueue.async { ... Task { @MainActor [weak self] in } }` to `async func` using `await bridge.requestAsync` (D-04, D-07).

Purpose: Wave-2 parallel batch. Zero file overlap with 49-02/03/05/06. External view callers (HealthSleepOverviewViews) are wrapped in 49-07.
Output: async `runSleepStaging`, `runReadinessV1`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-healthdatastore-async-migration/49-CONTEXT.md
@.planning/phases/49-healthdatastore-async-migration/49-RESEARCH.md
@.planning/phases/49-healthdatastore-async-migration/49-01-SUMMARY.md
@GooseSwift/GooseRustBridge.swift
@GooseSwift/HealthDataStore+StagingSleep.swift
@GooseSwift/HealthDataStore+Readiness.swift
</context>

## Artifacts this plan produces

- `HealthDataStore.runSleepStaging() async`
- `HealthDataStore.runReadinessV1() async`

<tasks>

<task type="auto">
  <name>Task 1: Migrate runSleepStaging to async (StagingSleep)</name>
  <files>GooseSwift/HealthDataStore+StagingSleep.swift</files>
  <read_first>
    - GooseSwift/HealthDataStore+StagingSleep.swift (whole file — `runSleepStaging` with bridge call at ~line 124; `packetInputQueue.async { ... Task { @MainActor [weak self] in } }` pattern)
    - GooseSwift/GooseRustBridge.swift (requestAsync from 49-01)
    - .planning/phases/49-healthdatastore-async-migration/49-RESEARCH.md (Pattern 2)
  </read_first>
  <action>
    Convert `runSleepStaging` to `func runSleepStaging() async`. Remove the `packetInputQueue.async { [weak self] in ... }` wrapper and the inner `Task { @MainActor [weak self] in ... }`. Capture @MainActor inputs (`databasePath`, staging args) into local constants before the await. Replace the `try bridge.request(...)` staging call with `try await bridge.requestAsync(...)`. After the await, mutate the staging result @Observable properties directly on `self`. Preserve existing error handling and result parsing. No `packetInputQueue` reference may remain.
  </action>
  <verify>
    <automated>grep -v '^[[:space:]]*//' GooseSwift/HealthDataStore+StagingSleep.swift | grep -c "await bridge.requestAsync"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^[[:space:]]*//' GooseSwift/HealthDataStore+StagingSleep.swift | grep -c "await bridge.requestAsync"` returns 1.
    - `grep -v '^[[:space:]]*//' GooseSwift/HealthDataStore+StagingSleep.swift | grep "bridge.request(" | grep -vc "await"` returns 0.
    - `grep -c "func runSleepStaging() async" GooseSwift/HealthDataStore+StagingSleep.swift` returns 1.
    - `grep -c "packetInputQueue" GooseSwift/HealthDataStore+StagingSleep.swift` returns 0.
  </acceptance_criteria>
  <done>runSleepStaging async, call awaited, no queue dispatch remains.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate runReadinessV1 to async with 2 sequential awaits (Readiness)</name>
  <files>GooseSwift/HealthDataStore+Readiness.swift</files>
  <read_first>
    - GooseSwift/HealthDataStore+Readiness.swift (whole file — `runReadinessV1` with 2 bridge calls at ~lines 101 and 151 within ONE packetInputQueue.async closure; first fetches exercise sessions, second calls goose_readiness_v1 using the first's result)
    - GooseSwift/GooseRustBridge.swift (requestAsync from 49-01)
    - .planning/phases/49-healthdatastore-async-migration/49-RESEARCH.md (Readiness 2-call note)
  </read_first>
  <action>
    Convert `runReadinessV1` to `func runReadinessV1() async`. Remove the `packetInputQueue.async { [weak self] in ... }` wrapper and the inner `Task { @MainActor [weak self] in ... }`. Capture @MainActor inputs before the first await. Replace BOTH `try bridge.request(...)` calls with `try await bridge.requestAsync(...)`, preserving order: the first await fetches the exercise-sessions report, then the second await calls `goose_readiness_v1` with args derived from the first result. Keep the data flow between the two calls identical. After the awaits, mutate the readiness @Observable result on `self` directly. Preserve existing error handling. No `packetInputQueue` reference may remain.
  </action>
  <verify>
    <automated>grep -v '^[[:space:]]*//' GooseSwift/HealthDataStore+Readiness.swift | grep -c "await bridge.requestAsync"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^[[:space:]]*//' GooseSwift/HealthDataStore+Readiness.swift | grep -c "await bridge.requestAsync"` returns 2.
    - `grep -v '^[[:space:]]*//' GooseSwift/HealthDataStore+Readiness.swift | grep "bridge.request(" | grep -vc "await"` returns 0.
    - `grep -c "func runReadinessV1() async" GooseSwift/HealthDataStore+Readiness.swift` returns 1.
    - `grep -c "packetInputQueue" GooseSwift/HealthDataStore+Readiness.swift` returns 0.
  </acceptance_criteria>
  <done>runReadinessV1 async, both calls awaited in order, no queue dispatch remains.</done>
</task>

</tasks>

<verification>
- 3 bridge calls across the two files awaited via requestAsync.
- Both run* methods async; no packetInputQueue usage in either file.
</verification>

<success_criteria>
- Sleep staging + Readiness migrated (ASYNC-01 progress); @MainActor mutations preserved (ASYNC-02).
</success_criteria>

<output>
Create `.planning/phases/49-healthdatastore-async-migration/49-04-SUMMARY.md` when done.
</output>
