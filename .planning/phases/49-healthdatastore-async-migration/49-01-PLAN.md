---
phase: 49-healthdatastore-async-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - GooseSwift/GooseRustBridge.swift
autonomous: true
requirements: [ASYNC-01]
must_haves:
  truths:
    - "GooseRustBridge exposes an async throws method that runs the FFI off the @MainActor"
    - "The project still builds after the additive change (sync request/requestValue still present)"
  artifacts:
    - path: "GooseSwift/GooseRustBridge.swift"
      provides: "requestAsync async wrapper over requestValue via Task.detached"
      contains: "func requestAsync"
  key_links:
    - from: "GooseSwift/GooseRustBridge.swift requestAsync"
      to: "requestValue (sync FFI)"
      via: "Task.detached(priority: .userInitiated).value"
      pattern: "Task\\.detached"
---

<objective>
Add an additive `requestAsync` (and `requestValueAsync`) method to `GooseRustBridge` that wraps the existing synchronous `requestValue` FFI call inside `Task.detached(priority: .userInitiated) { }.value`. This is the foundation for the wave-by-wave migration: the sync `request`/`requestValue` remain in place so every HealthDataStore extension file can be migrated one plan at a time without breaking the build mid-wave (D-06, D-01).

Purpose: Establish the async execution model at the bridge layer so the FFI never runs on @MainActor even when called from @MainActor context (D-01). This is wave 1 — no HealthDataStore file is touched yet.
Output: `GooseRustBridge.requestAsync(method:args:)` and `GooseRustBridge.requestValueAsync(method:args:)` async throws methods.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-healthdatastore-async-migration/49-CONTEXT.md
@.planning/phases/49-healthdatastore-async-migration/49-RESEARCH.md
@GooseSwift/GooseRustBridge.swift
</context>

## Artifacts this phase produces (Plan 01 contributions)

- `GooseRustBridge.requestAsync(method:args:) async throws -> [String: Any]` — new method
- `GooseRustBridge.requestValueAsync(method:args:) async throws -> Any` — new method (parallel to sync `requestValue`)
- `nonisolated(unsafe)` annotation on `lastTiming` (if needed to silence the worker-thread write data-race warning — see RISK-01 / Open Question 2)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add requestAsync / requestValueAsync async wrappers to GooseRustBridge</name>
  <files>GooseSwift/GooseRustBridge.swift</files>
  <read_first>
    - GooseSwift/GooseRustBridge.swift (whole file — current sync `request` at line 26, `requestValue` at line 30, `@unchecked Sendable` class at line 22, `lastTiming` at line 24)
    - GooseSwift/GooseSwift-Bridging-Header.h (C symbols `goose_bridge_handle_json` / `goose_bridge_free_string`)
  </read_first>
  <behavior>
    - After the change, an `async throws` call path exists that executes the blocking FFI on a detached worker thread, NOT on @MainActor.
    - The existing synchronous `request` and `requestValue` remain unchanged and still compile (additive — D-06).
    - `requestAsync` returns the same `[String: Any]` shape as the sync `request`.
    - `requestValueAsync` returns the same `Any` shape as the sync `requestValue` (needed for callers that read non-dictionary results).
  </behavior>
  <action>
    Add two methods to the `GooseRustBridge` class (the `final class GooseRustBridge: @unchecked Sendable` at line 22), placed immediately after the existing sync `requestValue` method (ends line 81):

    1. `func requestValueAsync(method: String, args: [String: Any] = [:]) async throws -> Any` — body: `try await Task.detached(priority: .userInitiated) { try self.requestValue(method: method, args: args) }.value`. Capturing `self` is safe because the class is `@unchecked Sendable`.

    2. `func requestAsync(method: String, args: [String: Any] = [:]) async throws -> [String: Any]` — body: `try await requestValueAsync(method: method, args: args) as? [String: Any] ?? [:]` (mirrors the sync `request` wrapper at line 26-28 but over the async variant).

    Do NOT modify, rename, or remove the existing sync `request` (line 26) or `requestValue` (line 30) — they stay until the final cleanup plan (49-07).

    `lastTiming` (line 24) is now written inside the detached task body (via `requestValue`) and is `private(set)` readable from @MainActor — this is a worker-thread write / main-actor read. If Swift strict-concurrency flags this with a warning during build, change the declaration to `nonisolated(unsafe) private(set) var lastTiming: GooseRustBridgeTiming?`. The `nonisolated(unsafe)` pattern is already used elsewhere in the codebase. Do not remove `lastTiming` in this plan (removal, if any, is deferred to 49-07).

    No fenced code in production beyond these two method bodies. Do not introduce any new dependency. Do not change the `counter` handling.
  </action>
  <verify>
    <automated>grep -c "func requestAsync\|func requestValueAsync" GooseSwift/GooseRustBridge.swift</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "func requestAsync" GooseSwift/GooseRustBridge.swift` returns exactly one match.
    - `grep -n "func requestValueAsync" GooseSwift/GooseRustBridge.swift` returns exactly one match.
    - `grep -n "Task.detached(priority: .userInitiated)" GooseSwift/GooseRustBridge.swift` returns at least one match inside `requestValueAsync`.
    - `grep -c "func requestValue(method: String" GooseSwift/GooseRustBridge.swift` returns 1 (the sync version is still present, unchanged).
    - `grep -c "func request(method: String" GooseSwift/GooseRustBridge.swift` returns 1 (sync `request` still present).
  </acceptance_criteria>
  <done>Both async methods exist, sync methods are untouched, and the file is additive-only.</done>
</task>

<task type="auto">
  <name>Task 2: Build verification — additive change compiles cleanly</name>
  <files>GooseSwift/GooseRustBridge.swift</files>
  <read_first>
    - GooseSwift/GooseRustBridge.swift (the edited file)
  </read_first>
  <action>
    Run an iOS Simulator build of the GooseSwift scheme to confirm the additive async methods compile with zero errors and zero new Swift Concurrency warnings (D-08a). Use XcodeBuildMCP `build_sim` for the GooseSwift scheme targeting an available iPhone simulator (e.g. iPhone 16). No HealthDataStore files are touched in this plan, so the build must remain green. If the build surfaces a `lastTiming` data-race warning, apply the `nonisolated(unsafe)` fix described in Task 1 and rebuild.
  </action>
  <verify>
    <automated>xcodebuild build -scheme GooseSwift -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - Build output ends with `** BUILD SUCCEEDED **`.
    - No compiler error referencing `GooseRustBridge.swift`.
    - No new "Publishing changes from background threads" or actor-isolation warning attributable to the new methods.
  </acceptance_criteria>
  <done>GooseSwift scheme builds successfully on iOS Simulator with the additive async methods present.</done>
</task>

</tasks>

<verification>
- `requestAsync` and `requestValueAsync` exist and wrap `Task.detached`.
- Sync `request` / `requestValue` unchanged (additive guarantee for wave migration).
- GooseSwift scheme builds clean on iOS Simulator.
</verification>

<success_criteria>
- GooseRustBridge exposes async FFI wrappers (foundation for ASYNC-01).
- No HealthDataStore file modified (wave isolation respected).
- Build green.
</success_criteria>

<output>
Create `.planning/phases/49-healthdatastore-async-migration/49-01-SUMMARY.md` when done
</output>
