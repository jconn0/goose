---
phase: 60-band-first-sync-align-goose-ble-sync-architecture-with-whoop
plan: 03
type: execute
wave: 2
depends_on:
  - 60-01
  - 60-02
files_modified:
  - GooseSwift/GooseAppModel+Lifecycle.swift
  - GooseSwift/GooseAppModel+NotificationPipeline.swift
  - GooseSwift/GooseAppModel+ActivityTimeline.swift
  - GooseSwift/MoreCaptureViews.swift
  - GooseSwift/CoachLocalToolContext.swift
  - GooseSwift/CodexCoachSupport.swift
  - GooseSwift/LocalizedStatusStrings.swift
autonomous: false
requirements: []
must_haves:
  truths:
    - "handleAppLifecycleChange handles lifecycle changes and calls triggerForegroundBLESync on active (no overnight guard gate)"
    - "handleBLEConnectionStateChange no longer references overnight readiness and keeps morning sleep sync"
    - "The Overnight Guard section is gone from the More tab"
    - "No source file references any removed overnight symbol; the project builds clean"
  artifacts:
    - path: "GooseSwift/GooseAppModel+Lifecycle.swift"
      provides: "Rewritten lifecycle handler wiring the foreground band-first sync"
      contains: "triggerForegroundBLESync()"
    - path: "GooseSwift/MoreCaptureViews.swift"
      provides: "Capture view with overnight guard section removed"
  key_links:
    - from: "GooseSwift/GooseAppModel+Lifecycle.swift"
      to: "GooseAppModel.triggerForegroundBLESync"
      via: "handleAppLifecycleChange active branch (D-08)"
      pattern: "triggerForegroundBLESync\\(\\)"
    - from: "GooseSwift/GooseAppModel+Lifecycle.swift"
      to: "GooseAppModel.maybeScheduleMorningSleepSync"
      via: "handleBLEConnectionStateChange ready branch (kept)"
      pattern: "maybeScheduleMorningSleepSync\\(\\)"
---

<objective>
Complete the atomic overnight removal: rewrite handleAppLifecycleChange to wire triggerForegroundBLESync() (D-08), clean handleBLEConnectionStateChange and delete the three overnight critical-background-task helpers from Lifecycle.swift, remove the overnightGuardActive parameter from the NotificationPipeline static methods and their callers, delete the two static overnight directory helpers from ActivityTimeline.swift, remove the Overnight Guard UI section + computed properties from MoreCaptureViews.swift (D-02), strip overnight dict keys from the two Coach context files, and remove the localizedOvernightGuardStatus section. Then verify the entire project builds clean with zero remaining overnight references.

Purpose: This is the wave-2 integration plan that makes the deletion (60-01) and the addition (60-02) compile together as one coherent change. After this plan the app uses the band-first sync path exclusively and no overnight symbol survives.
Output: Seven modified files, a clean iOS build, and a simulator verification that the More tab no longer shows an Overnight Guard section.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-band-first-sync-align-goose-ble-sync-architecture-with-whoop/60-CONTEXT.md
@.planning/phases/60-band-first-sync-align-goose-ble-sync-architecture-with-whoop/60-RESEARCH.md
@.planning/phases/60-band-first-sync-align-goose-ble-sync-architecture-with-whoop/60-PATTERNS.md
@GooseSwift/GooseAppModel+SleepSync.swift
</context>

<artifacts_this_phase_produces>
See plan 60-01 for the full phase artifact list. This plan (60-03) produces no NEW public symbols — it rewires existing call sites to the symbols created in 60-02 and removes the remaining overnight references. The only new call site introduced is `triggerForegroundBLESync()` inside `handleAppLifecycleChange` (the method itself is created in 60-02).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Lifecycle.swift handlers and delete overnight critical-task helpers</name>
  <files>GooseSwift/GooseAppModel+Lifecycle.swift</files>
  <read_first>
    - GooseSwift/GooseAppModel+Lifecycle.swift (read in full — current handleAppLifecycleChange wrapped in `guard overnightGuardActive else { return }`, handleBLEConnectionStateChange with the overnight block + refreshOvernightReadiness calls, and the three begin/expire/end critical-background-task helpers)
    - GooseSwift/GooseAppModel+BandFirstSync.swift (the triggerForegroundBLESync method created in 60-02 — confirm signature so the call site matches)
    - .planning/phases/60-band-first-sync-align-goose-ble-sync-architecture-with-whoop/60-RESEARCH.md ("GooseSwift/GooseAppModel+Lifecycle.swift" subsection, Pitfall 1, Pitfall 2)
    - .planning/phases/60-band-first-sync-align-goose-ble-sync-architecture-with-whoop/60-PATTERNS.md ("GooseAppModel+Lifecycle.swift (MODIFY)" section — exact before/after for both handlers and the methods to delete)
  </read_first>
  <action>
    Rewrite `handleAppLifecycleChange(_ phase: String)` from scratch (do NOT surgically edit — Pitfall 1): remove the `let power = Self.currentOvernightPowerState()` call (that method lived in the deleted OvernightRecovery.swift) and the `guard overnightGuardActive else { return }` that currently wraps the whole body. New body: log `ble.record(source: "app.lifecycle", title: "scene_phase", body: phase)`; then `if phase == "active" || phase == "foreground" { triggerHealthCheckIfNeeded(); triggerForegroundBLESync() }` (D-08 — the active branch wires the new band-first trigger created in 60-02). Remove the `applyOvernightPowerState`, `resumeOvernightGuardStreamsIfReady`, and `writeOvernightGuardStatus` calls.

    Clean `handleBLEConnectionStateChange(_ state:)`: remove the entire `if overnightGuardActive { ... return }` block and every `refreshOvernightReadiness(reason:)` call (those resolve to the deleted OvernightState.swift — Pitfall 2). KEEP the device-generation + captureFrameWriteQueue setup, the `guard state == "ready"` branch (keep passiveActivityCaptureWorkItem?.cancel()), schedulePassiveActivityCapture, scheduleAutoStartRespiratoryPacketWatchIfNeeded, the `ble.canSyncClock` / ble.writeClockCommand block, and `maybeScheduleMorningSleepSync()`.

    Delete the three overnight critical-background-task helper methods: `beginOvernightGuardCriticalBackgroundTask(reason:)`, `expireOvernightGuardCriticalBackgroundTask()`, `endOvernightGuardCriticalBackgroundTask()`.
  </action>
  <verify>
    <automated>grep -v '^[[:space:]]*//' GooseSwift/GooseAppModel+Lifecycle.swift | grep -c "overnightGuard\|refreshOvernightReadiness\|currentOvernightPowerState"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^[[:space:]]*//' GooseSwift/GooseAppModel+Lifecycle.swift | grep -c "overnightGuard"` returns 0
    - `grep -c "refreshOvernightReadiness\|currentOvernightPowerState\|resumeOvernightGuardStreamsIfReady\|writeOvernightGuardStatus\|applyOvernightPowerState" GooseSwift/GooseAppModel+Lifecycle.swift` returns 0
    - `grep -c "triggerForegroundBLESync()" GooseSwift/GooseAppModel+Lifecycle.swift` returns 1
    - `grep -c "triggerHealthCheckIfNeeded()" GooseSwift/GooseAppModel+Lifecycle.swift` returns 1 or more (preserved)
    - `grep -c "maybeScheduleMorningSleepSync()" GooseSwift/GooseAppModel+Lifecycle.swift` returns 1 (preserved)
    - `grep -c "beginOvernightGuardCriticalBackgroundTask\|expireOvernightGuardCriticalBackgroundTask\|endOvernightGuardCriticalBackgroundTask" GooseSwift/GooseAppModel+Lifecycle.swift` returns 0
  </acceptance_criteria>
  <done>Lifecycle.swift: handleAppLifecycleChange calls triggerForegroundBLESync on active and is not a no-op; handleBLEConnectionStateChange keeps morning sleep sync and drops overnight readiness; the three critical-task helpers are gone; zero overnight references remain.</done>
</task>

<task type="auto">
  <name>Task 2: Remove overnightGuardActive from NotificationPipeline + ActivityTimeline helpers</name>
  <files>GooseSwift/GooseAppModel+NotificationPipeline.swift, GooseSwift/GooseAppModel+ActivityTimeline.swift</files>
  <read_first>
    - GooseSwift/GooseAppModel+NotificationPipeline.swift (read the NotificationParseContext struct, notificationParseContext(for:) factory, the two static methods requiresMainParsedFrameHandling and canHandleDataSignalOffMain, and their two call sites)
    - GooseSwift/GooseAppModel+ActivityTimeline.swift (read the two static overnight directory methods near lines 668–679)
    - .planning/phases/60-band-first-sync-align-goose-ble-sync-architecture-with-whoop/60-RESEARCH.md ("GooseAppModel+NotificationPipeline.swift" and "GooseAppModel+ActivityTimeline.swift" subsections, Pitfall 7)
    - .planning/phases/60-band-first-sync-align-goose-ble-sync-architecture-with-whoop/60-PATTERNS.md (NotificationPipeline + ActivityTimeline MODIFY sections — exact parameter/branch removals)
  </read_first>
  <action>
    In GooseSwift/GooseAppModel+NotificationPipeline.swift:
    - Remove the `overnightGuardActive: Bool` field from `struct NotificationParseContext`.
    - Remove `overnightGuardActive: overnightGuardActive` from the `notificationParseContext(for:)` factory init.
    - Change `requiresMainParsedFrameHandling(_:overnightGuardActive:)` to `requiresMainParsedFrameHandling(_:)`: drop the parameter and remove the `if overnightGuardActive, let packetType { ... }` branch (the packetType 47/49/56 cases stay handled by the existing conditions above it).
    - Change `canHandleDataSignalOffMain(_:overnightGuardActive:respiratoryPacketWatchActive:)` to `canHandleDataSignalOffMain(_:respiratoryPacketWatchActive:)`: drop the parameter and remove `!overnightGuardActive` from the guard while keeping `!respiratoryPacketWatchActive`.
    - Update BOTH call sites of these two static methods to drop the `overnightGuardActive:` argument (Pitfall 7: the compile error surfaces at the call site, so both must change with the definition).

    In GooseSwift/GooseAppModel+ActivityTimeline.swift:
    - Delete the two static methods `overnightGuardDirectoryURL(sessionID:)` and `overnightGuardRootDirectoryURL()`. Their only callers were the deleted OvernightRun.swift / OvernightRecovery.swift.
  </action>
  <verify>
    <automated>grep -c "overnightGuardActive\|overnightGuardDirectoryURL\|overnightGuardRootDirectoryURL" GooseSwift/GooseAppModel+NotificationPipeline.swift GooseSwift/GooseAppModel+ActivityTimeline.swift</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "overnightGuardActive" GooseSwift/GooseAppModel+NotificationPipeline.swift` returns 0
    - `grep -c "overnightGuardActive:" GooseSwift/GooseAppModel+NotificationPipeline.swift` returns 0 (no parameter or argument remains)
    - `grep -c "overnightGuardDirectoryURL\|overnightGuardRootDirectoryURL" GooseSwift/GooseAppModel+ActivityTimeline.swift` returns 0
    - `grep -c "requiresMainParsedFrameHandling" GooseSwift/GooseAppModel+NotificationPipeline.swift` returns 3 (1 definition + 2 callers, all updated)
  </acceptance_criteria>
  <done>NotificationPipeline static methods and their callers no longer take overnightGuardActive; NotificationParseContext drops the field; ActivityTimeline's two overnight directory helpers are deleted.</done>
</task>

<task type="auto">
  <name>Task 3: Remove overnight UI and context references; build clean</name>
  <files>GooseSwift/MoreCaptureViews.swift, GooseSwift/CoachLocalToolContext.swift, GooseSwift/CodexCoachSupport.swift, GooseSwift/LocalizedStatusStrings.swift</files>
  <read_first>
    - GooseSwift/MoreCaptureViews.swift (read the `Section("Overnight Guard") { ... }` block and the four private computed properties: overnightGuardStatus, overnightGuardReadinessStatus, overnightGuardSQLiteMirrorStatus, overnightGuardExportStatus)
    - GooseSwift/CoachLocalToolContext.swift (read the overnight_guard dict keys in the context snapshot ~lines 127–132)
    - GooseSwift/CodexCoachSupport.swift (read the overnight_guard dict keys ~lines 120–129)
    - GooseSwift/LocalizedStatusStrings.swift (read the `MARK: - Overnight Guard Status` section / localizedOvernightGuardStatus on String ~lines 185–198)
    - .planning/phases/60-band-first-sync-align-goose-ble-sync-architecture-with-whoop/60-RESEARCH.md (MoreCaptureViews, CoachLocalToolContext, CodexCoachSupport, LocalizedStatusStrings subsections)
    - .planning/phases/60-band-first-sync-align-goose-ble-sync-architecture-with-whoop/60-PATTERNS.md (Deletion Checklist — zero-occurrence search terms)
  </read_first>
  <action>
    In GooseSwift/MoreCaptureViews.swift: remove the entire `Section("Overnight Guard") { ... }` block (all MoreInfoRow displays, the Start Guard / Final Sync / Stop Guard buttons, and the ShareLink exports) per D-02 — no replacement UI. Remove the four private computed properties `overnightGuardStatus`, `overnightGuardReadinessStatus`, `overnightGuardSQLiteMirrorStatus`, `overnightGuardExportStatus`.

    In GooseSwift/CoachLocalToolContext.swift: remove the entire `overnight_guard` key and its nested value dict (the active/status/readiness/targets/last_packet/spool entries) from the context snapshot.

    In GooseSwift/CodexCoachSupport.swift: remove the entire `overnight_guard` key and its nested value dict (active/status/readiness/raw_notifications/target/last_packet/spool/sqlite_mirror/power/watchdog) from the context snapshot.

    In GooseSwift/LocalizedStatusStrings.swift: remove the `MARK: - Overnight Guard Status` section and the `localizedOvernightGuardStatus` computed property on String (its only caller, MoreCaptureViews line ~81, is removed above).

    After all edits across this plan, build the iOS project for the simulator and confirm zero compile errors. Run a repo-wide grep sweep for every deletion-checklist term and confirm zero matches in GooseSwift/ (excluding .planning/).
  </action>
  <verify>
    <automated>cd /Users/francisco/Documents/goose && grep -rl "overnightGuardActive\|refreshOvernightReadiness\|currentOvernightPowerState\|persistOvernightRawNotificationBeforeInterpretation\|persistOvernightCommandWrite\|persistOvernightEventLog\|recoverUncleanOvernightGuardSessionIfNeeded\|OvernightGuardSession\|OvernightGuardTargetCounts\|localizedOvernightGuardStatus\|Section(\"Overnight Guard\")" GooseSwift/ | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Section(\"Overnight Guard\")" GooseSwift/MoreCaptureViews.swift` returns 0
    - `grep -c "overnightGuard" GooseSwift/MoreCaptureViews.swift` returns 0
    - `grep -c "overnight_guard\|overnightGuard" GooseSwift/CoachLocalToolContext.swift` returns 0
    - `grep -c "overnight_guard\|overnightGuard" GooseSwift/CodexCoachSupport.swift` returns 0
    - `grep -c "localizedOvernightGuardStatus" GooseSwift/LocalizedStatusStrings.swift` returns 0
    - Repo-wide sweep (the verify command) returns 0 — no GooseSwift/ file references any removed overnight symbol
    - iOS simulator build succeeds with zero `error:` lines (build the GooseSwift scheme via XcodeBuildMCP build_sim or `xcodebuild ... build 2>&1 | grep -c "error:"` returns 0)
  </acceptance_criteria>
  <done>Overnight UI section and the four computed properties are gone from MoreCaptureViews; both Coach context files drop the overnight_guard dict; localizedOvernightGuardStatus is removed; the repo-wide overnight sweep returns zero; the project builds clean for the simulator.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify More tab no longer shows Overnight Guard</name>
  <files>GooseSwift/MoreCaptureViews.swift</files>
  <action>
    Human-verify checkpoint (no code change). The executor builds and runs the app on an iOS simulator, navigates to the More tab Capture screen, and confirms the Overnight Guard section is absent and the app runs cleanly after the band-first sync wiring. Capture a screenshot for the SUMMARY. This checkpoint gates phase completion.
  </action>
  <what-built>The overnight guard subsystem is fully removed and replaced by the band-first foreground/background sync. The More tab Capture screen no longer contains an "Overnight Guard" section, and the app launches normally with the new band-first sync wiring.</what-built>
  <how-to-verify>
    1. Build and run the GooseSwift app on an iOS simulator (XcodeBuildMCP build_run_sim).
    2. Confirm the app launches to the main shell without crashing.
    3. Navigate: More tab → Capture screen.
    4. Confirm there is NO "Overnight Guard" section (no Start Guard / Final Sync / Stop Guard buttons, no overnight status rows, no overnight export ShareLinks).
    5. Capture a screenshot of the More/Capture screen for the SUMMARY.
    6. Optionally, in the LLDB console with the app paused, simulate the background task to confirm the handler is reachable: `e -l objc -- (void)[[BGTaskScheduler sharedScheduler] _simulateLaunchForTaskWithIdentifier:@"com.goose.swift.bg-sync"]` and confirm no crash (handler reschedules + completes).
  </how-to-verify>
  <verify>
    <human-check>Overnight Guard section absent from More tab; app runs without crash; screenshot captured.</human-check>
  </verify>
  <done>Human verifier confirms the Overnight Guard section is gone and the app runs cleanly under the band-first sync model.</done>
  <resume-signal>Type "approved" if the Overnight Guard section is absent and the app runs cleanly, or describe any issue (crash, lingering overnight UI, build error).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OS lifecycle → app | scenePhase active triggers the band-first foreground sync via handleAppLifecycleChange |
| background queue → static parse methods | NotificationPipeline static methods run nonisolated off the main actor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-07 | Denial of Service | handleAppLifecycleChange left as a no-op (overnight guard removed but guard kept) | mitigate | Method is rewritten from scratch (not surgically edited); acceptance_criteria assert triggerHealthCheckIfNeeded and triggerForegroundBLESync are both present in the active branch |
| T-60-08 | Tampering | Missed overnight call site causes a runtime/compile break | mitigate | Repo-wide grep sweep over the deletion-checklist terms must return 0 and the simulator build must produce zero error: lines before the plan completes |
| T-60-09 | Elevation of Privilege | NotificationPipeline static method signature change misses a caller | mitigate | Pitfall 7 explicitly enumerated; acceptance asserts requiresMainParsedFrameHandling resolves to 3 occurrences (definition + 2 updated callers) |
| T-60-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; dependency surface unchanged |
</threat_model>

<verification>
- Lifecycle.swift: handleAppLifecycleChange wires triggerForegroundBLESync; handleBLEConnectionStateChange keeps maybeScheduleMorningSleepSync; three critical-task helpers deleted.
- NotificationPipeline: overnightGuardActive removed from struct, factory, both static methods, and both callers.
- ActivityTimeline: two overnight directory helpers deleted.
- MoreCaptureViews / CoachLocalToolContext / CodexCoachSupport / LocalizedStatusStrings: all overnight references removed.
- Repo-wide overnight sweep returns 0; iOS simulator build has zero error: lines.
- Human checkpoint: More tab shows no Overnight Guard section; app runs clean.
</verification>

<success_criteria>
- Repo-wide grep of all deletion-checklist overnight terms across GooseSwift/ returns 0 files.
- `xcodebuild ... build 2>&1 | grep -c "error:"` returns 0 (simulator build clean).
- handleAppLifecycleChange contains exactly one triggerForegroundBLESync() call (D-08).
- Human verifier confirms the Overnight Guard section is absent from the More tab.
</success_criteria>

<output>
Create `.planning/phases/60-band-first-sync-align-goose-ble-sync-architecture-with-whoop/60-03-SUMMARY.md` when done
</output>
