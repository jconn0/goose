---
phase: 50-morning-band-sleep-sync
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - GooseSwift/GooseAppModel+SleepSync.swift
  - GooseSwift/GooseAppModel+Lifecycle.swift
  - GooseSwift/HealthDataStore.swift
  - GooseSwift/AppShellView.swift  # conditional — only if healthStore is not accessible via self in GooseAppModel extension
autonomous: true
requirements:
  - SLP-SYNC-02
  - SLP-SYNC-03

must_haves:
  truths:
    - "GooseAppModel+SleepSync.swift exists with syncBandSleepHistory() and maybeScheduleMorningSleepSync()"
    - "handleBLEConnectionStateChange calls maybeScheduleMorningSleepSync() when state==ready and overnightGuardActive==false"
    - "UserDefaults key goose.swift.last_band_sleep_sync_date is written at start of syncBandSleepHistory (not on completion)"
    - "syncBandSleepHistory uses store.gravity_rows_between for SQLite-first check (threshold 100 rows)"
    - "sleep_id is deterministic: band_ble.{deviceId}.{yyyy-MM-dd} to prevent duplicates on reconnect"
    - "bandSleepImportStatus initial value changed from 'No band sync yet' to 'A aguardar sincronização'"
    - "If sleep_staging returns staging_method no_imu_data, no session is inserted and status = 'A aguardar sincronização'"
    - "xcodebuild compiles without errors"
  artifacts:
    - path: "GooseSwift/GooseAppModel+SleepSync.swift"
      provides: "syncBandSleepHistory() async + maybeScheduleMorningSleepSync() + overnightWindow() + bandSleepId()"
      min_lines: 80
    - path: "GooseSwift/GooseAppModel+Lifecycle.swift"
      provides: "maybeScheduleMorningSleepSync() call in handleBLEConnectionStateChange"
      contains: "maybeScheduleMorningSleepSync"
    - path: "GooseSwift/HealthDataStore.swift"
      provides: "bandSleepImportStatus initial value 'A aguardar sincronização'"
      contains: "A aguardar sincronização"
  key_links:
    - from: "GooseAppModel+Lifecycle.swift handleBLEConnectionStateChange"
      to: "GooseAppModel+SleepSync.swift maybeScheduleMorningSleepSync"
      via: "direct call after existing overnight guard check"
      pattern: "maybeScheduleMorningSleepSync"
    - from: "syncBandSleepHistory"
      to: "store.gravity_rows_between bridge"
      via: "bridge.requestAsync"
      pattern: "gravity_rows_between"
    - from: "syncBandSleepHistory"
      to: "sleep.import_external_history bridge"
      via: "bridge.requestAsync after staging result"
      pattern: "import_external_history"
---

<objective>
Create GooseAppModel+SleepSync.swift with the morning sync trigger and full syncBandSleepHistory() flow — wiring the SQLite-first gravity check, optional BLE historical sync, sleep staging bridge call, and external_sleep_sessions insert. Update handleBLEConnectionStateChange to call maybeScheduleMorningSleepSync(). Change initial bandSleepImportStatus to pt-PT string.

Purpose: SLP-SYNC-02 (morning auto-sync) + SLP-SYNC-03 (pt-PT label on Sleep V2 dashboard).
Output: New GooseAppModel+SleepSync.swift, 2-line change in +Lifecycle.swift, 1-line change in HealthDataStore.swift.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-morning-band-sleep-sync/50-CONTEXT.md
@.planning/phases/50-morning-band-sleep-sync/50-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: GooseAppModel+SleepSync.swift — full sync flow</name>
  <files>GooseSwift/GooseAppModel+SleepSync.swift</files>

  <read_first>
    Read GooseSwift/GooseAppModel.swift lines 65–100 (onHistoricalSyncCompleted var at line 72; ble, rust, healthStore properties — confirm property names).
    Read GooseSwift/GooseAppModel+Lifecycle.swift lines 115–166 (handleBLEConnectionStateChange — exact injection point; overnightGuardActive usage).
    Read GooseSwift/GooseAppModel+OvernightRun.swift lines 1–40 (extension pattern on GooseAppModel — use as structural template for new file).
    Read GooseSwift/HealthDataStore.swift lines 270–295 (markBandSleepSyncRequested, markBandSleepSyncFailed, refreshSleepAfterBandSync signatures).
    Read GooseSwift/GooseBLEClient.swift lines 865–880 (canSyncHistorical guard — check property name exactly).
    Read GooseSwift/GooseBLEClient+UserActions.swift (find syncHistoricalPackets(rangeFirst:) signature).
    Read GooseSwift/GooseRustBridge.swift lines 80–100 (requestAsync method signature).
  </read_first>

  <action>
  Create GooseSwift/GooseAppModel+SleepSync.swift as an extension on GooseAppModel. Follow the established extension pattern (two blank lines between top-level declarations, K&R brace style, 2-space indent).

  The file must contain:

  1. A `DefaultsKey` extension or a static constant for the UserDefaults key. The project pattern uses nested static let on the enclosing type; add to an existing `DefaultsKey` struct if one exists in GooseAppModel, or add it as a file-level private constant:
       static let lastBandSleepSyncDateKey = "goose.swift.last_band_sleep_sync_date"
     Check GooseAppModel.swift for whether a DefaultsKey struct already exists — if so, extend it. Do not create a duplicate.

  2. Static helper `bandSleepId(deviceId: String, overnightStartDate: Date) -> String`:
     Uses DateFormatter with format "yyyy-MM-dd" and TimeZone.current to format overnightStartDate, returns "band_ble.\(deviceId).\(dateStr)".

  3. Static helper `overnightWindow() -> (Double, Double)`:
     Returns (yesterdayEvening20h.timeIntervalSince1970, todayNoon12h.timeIntervalSince1970) using Calendar.current and dateComponents. See RESEARCH.md Code Examples section for the exact Calendar arithmetic (yesterday at 20:00 local, today at 12:00 local).

  4. `maybeScheduleMorningSleepSync()` — @MainActor, non-async, synchronous gate check:
     - Guard: `!overnightGuardActive` — return if overnight guard active (per CONTEXT.md decision).
     - Guard: `Calendar.current.component(.hour, from: Date()) >= 4` — return if before 04:00 local (morning window).
     - Guard: lastBandSleepSyncDate check — read UserDefaults.standard.object(forKey: key) as? Date; if not nil and Calendar.current.isDateInToday(lastBandSleepSyncDate), return (already synced today).
     - If all guards pass: launch `Task { await self.syncBandSleepHistory() }`.

  5. `syncBandSleepHistory()` — async, runs in detached Task context (bridge calls use await bridge.requestAsync which is Task.detached internally):
     a. Immediately write UserDefaults: `UserDefaults.standard.set(Date(), forKey: key)` — BEFORE any await, on @MainActor (per CONTEXT.md decision: "Escrita do UserDefaults: ao iniciar, para evitar retry loop").
     b. Call `await healthStore.markBandSleepSyncRequested(automatic: true, canSync: ble.canSyncHistorical, detail: "")` — use the exact signature from HealthDataStore.swift. If markBandSleepSyncRequested does not take these exact parameters, adapt to the actual signature.
     c. Compute overnight window: `let (overnightStart, overnightEnd) = Self.overnightWindow()`.
     d. Get deviceId: `let deviceId = ble.activeDeviceIdentifier?.uuidString ?? ""`. Guard that deviceId is not empty — if empty, call await healthStore.markBandSleepSyncFailed("No active device") and return.
     e. Get DB path: `let dbPath = HealthDataStore.defaultDatabasePath()`.
     f. SQLite-first check via bridge: call `store.gravity_rows_between` using bridge.requestAsync(method: "store.gravity_rows_between", args: ["database_path": dbPath, "device_id": deviceId, "ts_start": overnightStart, "ts_end": overnightEnd]). Extract gravity row count from result — check what key the bridge returns (likely "rows" array or "count" — read bridge.rs store.gravity_rows_between handler to confirm). If count < 100: trigger BLE sync (step g). If count >= 100: skip to step h.
     g. BLE historical sync (only if gravity < 100 rows):
        Guard `ble.canSyncHistorical` — if false, call markBandSleepSyncFailed and return.
        DO NOT use onHistoricalSyncCompleted for BLE sync coordination — use historicalSyncStatus polling as specified here. RESEARCH.md Pattern 2 code example uses onHistoricalSyncCompleted; that implementation path is shown for reference only and MUST NOT be followed here (causes Pitfall #3: AppShellView callback slot conflict).
        Use a Task-based observation of ble.historicalSyncStatus to avoid conflicting with onHistoricalSyncCompleted (per RESEARCH.md Pitfall #3 and Open Questions #2): launch `ble.syncHistoricalPackets(rangeFirst: true)` then poll historicalSyncStatus via a short Task.sleep loop (e.g., 1s intervals, max 120 attempts = 2 minutes) until it equals "complete" or "failed". If status becomes "failed" or loop times out, call markBandSleepSyncFailed and return. This avoids overwriting the onHistoricalSyncCompleted callback slot used by AppShellView.
     h. Run sleep staging: call bridge.requestAsync(method: "metrics.sleep_staging", args: ["database_path": dbPath, "device_id": deviceId, "sleep_start_ts": overnightStart, "sleep_end_ts": overnightEnd]). Extract stagingMethod from result["staging_method"] as? String. Per CONTEXT.md: if stagingMethod == "no_imu_data" (STAGING_METHOD_NO_IMU from sleep_staging.rs), set `await MainActor.run { healthStore.bandSleepImportStatus = "A aguardar sincronização" }` and return without inserting.
     i. Build sleep session and insert: construct deterministic sleepId using Self.bandSleepId(deviceId: deviceId, overnightStartDate: Date(timeIntervalSince1970: overnightStart)). Call bridge.requestAsync(method: "sleep.import_external_history", args: [...]). The args must include: sleep_id, source="band_ble", platform="goose_ble", platform_record_id=sleepId, start_time_unix_ms=Int64(overnightStart * 1000), end_time_unix_ms=Int64(overnightEnd * 1000), confidence=0.7, provenance=["source":"band_ble","auto_sync":true]. Extract stage_minutes from staging result if available and include in stage_summary arg.
     j. On success: call `await healthStore.refreshSleepAfterBandSync(packetCount: 0)` then `await MainActor.run { healthStore.bandSleepImportStatus = "Sincronizado da pulseira" }`.
     k. Wrap the entire body in do { ... } catch { await healthStore.markBandSleepSyncFailed("Sleep sync error: \(error)") } — bridge.requestAsync may throw.

  All bridge calls must use `bridge.requestAsync` (never `bridge.request` — per Phase 49 async migration, anti-pattern documented in RESEARCH.md). The `bridge` property accessible in GooseAppModel extensions is `self.rust` (the GooseRustBridge instance at GooseAppModel.swift line 78).

  The healthStore property: GooseAppModel does not own HealthDataStore directly — it is passed via callback. Before writing any code, read GooseAppModel.swift to verify if healthStore is accessible via `self` in this extension (GooseAppModel+SleepSync.swift is an extension on GooseAppModel which may have a stored healthStore reference set by AppShellView). Look for how other extensions call healthStore methods — e.g., GooseAppModel+OvernightRun.swift. If a direct reference exists, use it directly. If no direct reference exists, store a `weak var healthStore: HealthDataStore?` set by AppShellView alongside onHistoricalSyncCompleted, and update AppShellView.swift to set it (AppShellView.swift is listed in files_modified for this conditional case). Do not add a strong reference cycle.
  </action>

  <verify>
    <automated>cd /Users/francisco/Documents/goose && xcodebuild -project GooseSwift.xcodeproj -scheme GooseSwift -destination 'platform=iOS Simulator,name=iPhone 16,OS=latest' -only-testing: build 2>&1 | grep -E "error:|Build succeeded" | tail -20</automated>
  </verify>

  <done>GooseAppModel+SleepSync.swift compiles without errors. Contains: maybeScheduleMorningSleepSync(), syncBandSleepHistory() async, overnightWindow(), bandSleepId(). No strong reference cycles introduced.</done>
</task>

<task type="auto">
  <name>Task 2: Wire trigger in +Lifecycle.swift + initial string in HealthDataStore.swift</name>
  <files>GooseSwift/GooseAppModel+Lifecycle.swift, GooseSwift/HealthDataStore.swift</files>

  <read_first>
    Read GooseSwift/GooseAppModel+Lifecycle.swift lines 115–167 (handleBLEConnectionStateChange full body — find the exact line after which to add maybeScheduleMorningSleepSync()).
    Read GooseSwift/HealthDataStore.swift line 16 (bandSleepImportStatus = "No band sync yet" — the line to change).
  </read_first>

  <action>
  In GooseAppModel+Lifecycle.swift, in handleBLEConnectionStateChange, find the final closing block that runs when `state == "ready"` and overnightGuardActive == false (the guard at line ~154 passes). After the existing calls to refreshOvernightReadiness, schedulePassiveActivityCapture, scheduleAutoStartRespiratoryPacketWatchIfNeeded, and the canSyncClock block, add one call:

    maybeScheduleMorningSleepSync()

  The call goes at the END of the state == "ready" branch (after the clock sync block), so it only runs when the device is connected, not in overnight guard, and state is "ready". Per CONTEXT.md decision: "Observer connectionState em GooseAppModel — quando muda para 'connected' verificar condições". The actual state value for ready is "ready" (confirmed from GooseAppModel+Lifecycle.swift line 116 code read above).

  In HealthDataStore.swift, change line 16 from:
    var bandSleepImportStatus = "No band sync yet"
  to:
    var bandSleepImportStatus = "A aguardar sincronização"

  Per CONTEXT.md decision (SLP-SYNC-03): "Estado inicial / sem sync: 'A aguardar sincronização' (substituir 'No band sync yet' inicial)". Per RESEARCH.md State of the Art table: deprecated string confirmed.

  No other changes to these files.
  </action>

  <verify>
    <automated>cd /Users/francisco/Documents/goose && grep -n "maybeScheduleMorningSleepSync" GooseSwift/GooseAppModel+Lifecycle.swift && grep -n "A aguardar" GooseSwift/HealthDataStore.swift</automated>
  </verify>

  <done>grep confirms maybeScheduleMorningSleepSync appears in +Lifecycle.swift. grep confirms "A aguardar sincronização" appears in HealthDataStore.swift line 16. xcodebuild compiles cleanly (run verify from Task 1 again to confirm).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| @MainActor → async bridge | syncBandSleepHistory dispatches via requestAsync (Task.detached) — never blocks main thread |
| UserDefaults date guard → sync trigger | Date guard is best-effort anti-spam; deterministic sleep_id is the real idempotency guarantee |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-04 | Tampering | duplicate external_sleep_sessions on reconnect | mitigate | sleep_id = "band_ble.{deviceId}.{date}" is deterministic; UNIQUE(platform, platform_record_id) in store prevents second insert |
| T-50-05 | Denial of Service | syncBandSleepHistory BLE poll loop runs indefinitely | mitigate | max 120 poll attempts (2 min) with Task.sleep(1s); times out and calls markBandSleepSyncFailed |
| T-50-06 | Tampering | empty deviceId passed to bridge | mitigate | guard deviceId.isEmpty in syncBandSleepHistory before any bridge call |
| T-50-07 | Elevation | onHistoricalSyncCompleted callback race with AppShellView | mitigate | use historicalSyncStatus polling instead of setting onHistoricalSyncCompleted — avoids single-slot conflict |
| T-50-SC | Tampering | npm/pip/cargo installs | accept | no new packages — zero installs this plan |
</threat_model>

<verification>
Build check: xcodebuild -project GooseSwift.xcodeproj -scheme GooseSwift -destination 'platform=iOS Simulator,name=iPhone 16,OS=latest' build 2>&1 | grep -E "error:|Build succeeded"

String check: grep -n "A aguardar" GooseSwift/HealthDataStore.swift returns line 16.
Trigger check: grep -n "maybeScheduleMorningSleepSync" GooseSwift/GooseAppModel+Lifecycle.swift returns at least one match.
Method check: grep -rn "syncBandSleepHistory" GooseSwift/ returns GooseAppModel+SleepSync.swift.
</verification>

<success_criteria>
1. GooseAppModel+SleepSync.swift exists and compiles
2. maybeScheduleMorningSleepSync() is called from handleBLEConnectionStateChange when state=="ready" and overnightGuardActive==false
3. syncBandSleepHistory() writes UserDefaults at start (before any await)
4. SQLite-first gravity check calls store.gravity_rows_between with threshold 100
5. sleep_id is deterministic format "band_ble.{deviceId}.{yyyy-MM-dd}"
6. staging_method == "no_imu_data" guard prevents insert and sets "A aguardar sincronização"
7. On success: bandSleepImportStatus = "Sincronizado da pulseira"
8. bandSleepImportStatus initial value in HealthDataStore.swift is "A aguardar sincronização"
9. xcodebuild compiles without errors
10. No onHistoricalSyncCompleted slot conflict (uses historicalSyncStatus polling instead)
</success_criteria>

<output>
Create .planning/phases/50-morning-band-sleep-sync/50-02-SUMMARY.md when done.
</output>

## Artifacts this phase produces

| Symbol / File | Type | Consumed by |
|---------------|------|-------------|
| `GooseAppModel+SleepSync.swift` | New Swift file | GooseAppModel+Lifecycle.swift (calls maybeScheduleMorningSleepSync) |
| `maybeScheduleMorningSleepSync()` | @MainActor method | handleBLEConnectionStateChange |
| `syncBandSleepHistory()` | async method | Task spawned from maybeScheduleMorningSleepSync |
| `overnightWindow() -> (Double, Double)` | static helper | syncBandSleepHistory |
| `bandSleepId(deviceId:overnightStartDate:) -> String` | static helper | syncBandSleepHistory (deterministic sleep_id) |
| `GooseAppModel+Lifecycle.swift` maybeScheduleMorningSleepSync call | Code change | morning auto-sync trigger |
| `HealthDataStore.swift` bandSleepImportStatus initial value | String change | SleepV2BandSyncCard displays "A aguardar sincronização" on first load |
</output>
