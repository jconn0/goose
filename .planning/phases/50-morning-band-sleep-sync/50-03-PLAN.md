---
phase: 50-morning-band-sleep-sync
plan: 03
type: execute
wave: 2
depends_on:
  - "50-01"
  - "50-02"
files_modified: []
autonomous: false
requirements:
  - SLP-SYNC-01
  - SLP-SYNC-02
  - SLP-SYNC-03

must_haves:
  truths:
    - "cargo test -p goose-core passes with zero failures"
    - "xcodebuild compiles without errors"
    - "Sleep V2 dashboard shows 'A aguardar sincronização' on first launch (before any sync)"
    - "bandSleepImportStatus is readable in SleepV2BandSyncCard"
  artifacts:
    - path: "Rust/core/src/bridge.rs"
      provides: "gravity extraction wired (from plan 01)"
      contains: "insert_gravity_rows"
    - path: "GooseSwift/GooseAppModel+SleepSync.swift"
      provides: "syncBandSleepHistory (from plan 02)"
      contains: "syncBandSleepHistory"
    - path: "GooseSwift/HealthDataStore.swift"
      provides: "pt-PT initial status string (from plan 02)"
      contains: "A aguardar sincronização"
  key_links:
    - from: "gravity table (SQLite)"
      to: "metrics.sleep_staging bridge"
      via: "gravity_rows_between read inside sleep_staging_bridge"
      pattern: "sleep_staging"
    - from: "bandSleepImportStatus"
      to: "SleepV2BandSyncCard"
      via: "@Observable binding"
      pattern: "bandSleepImportStatus"
---

<objective>
Validate the full phase 50 pipeline: Rust tests green, Swift build clean, and visual confirmation that the Sleep V2 dashboard shows the correct initial status and is wired to receive sync results.

Purpose: Phase gate — all three requirements (SLP-SYNC-01, SLP-SYNC-02, SLP-SYNC-03) verified before marking phase complete.
Output: No new files. Human approval that the simulator shows the correct status label.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-morning-band-sleep-sync/50-CONTEXT.md
@.planning/phases/50-morning-band-sleep-sync/50-RESEARCH.md
@.planning/phases/50-morning-band-sleep-sync/50-01-SUMMARY.md
@.planning/phases/50-morning-band-sleep-sync/50-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full cargo test suite + xcodebuild clean compile</name>
  <files>Rust/core/src/bridge.rs (if Rust regression), Rust/core/tests/bridge_tests.rs (if test regression), GooseSwift/GooseAppModel+SleepSync.swift (if Swift compile error)</files>

  <read_first>
    No files to read — this task runs verification commands only. All implementation is from plans 01 and 02.
  </read_first>

  <action>
  Run the full Rust test suite:
    cd /Users/francisco/Documents/goose/Rust/core && cargo test -p goose-core 2>&1

  If any test fails, read the failure output and determine whether it is:
    (a) A regression from the bridge.rs changes in plan 01 — fix in bridge.rs (same file, allowed since plan 01 is already complete and this plan has no files_modified overlap).
    (b) A failure in a new test from plan 01 — fix in bridge_tests.rs.

  Then run xcodebuild to confirm Swift compilation:
    cd /Users/francisco/Documents/goose && xcodebuild -project GooseSwift.xcodeproj -scheme GooseSwift -destination 'platform=iOS Simulator,name=iPhone 16,OS=latest' build 2>&1 | grep -E "error:|warning:|Build succeeded|Build failed"

  If compilation errors exist, diagnose and fix in the file that contains the error (GooseAppModel+SleepSync.swift or GooseAppModel+Lifecycle.swift — both from plan 02 which is already complete).

  Report results to the user before the checkpoint.
  </action>

  <verify>
    <automated>cd /Users/francisco/Documents/goose/Rust/core && cargo test -p goose-core 2>&1 | tail -5 && cd /Users/francisco/Documents/goose && xcodebuild -project GooseSwift.xcodeproj -scheme GooseSwift -destination 'platform=iOS Simulator,name=iPhone 16,OS=latest' build 2>&1 | grep -E 'error:|Build succeeded|Build failed' | tail -10</automated>
  </verify>

  <done>cargo test output shows "test result: ok. N passed; 0 failed". xcodebuild shows "Build succeeded".</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
  Plans 01 and 02 delivered:
  - bridge.rs: gravity_x/y/z extracted from K18/K24 V24History frames and inserted into SQLite gravity table via store.insert_gravity_rows — previously ignored with `..`
  - bridge_tests.rs: 4 new tests (gravity extraction, roundtrip insert, external sleep session insert, idempotency)
  - GooseAppModel+SleepSync.swift: full syncBandSleepHistory() flow with SQLite-first gravity check (100-row threshold), optional BLE historical sync, sleep staging, external_sleep_sessions insert
  - maybeScheduleMorningSleepSync() wired into handleBLEConnectionStateChange (runs when state==ready, !overnightGuardActive, hour>=4, not synced today)
  - bandSleepImportStatus initial value changed to "A aguardar sincronização"
  </what-built>

  <how-to-verify>
  1. Build and launch the app in the iOS simulator:
     Open GooseSwift.xcodeproj in Xcode → Product → Run (or use the xcodebuild command from Task 1)

  2. Navigate to the Sleep tab (Sleep V2 dashboard).

  3. Find the SleepV2BandSyncCard — it should show a row titled "Sleep score" with the value from bandSleepImportStatus.
     Expected value: "A aguardar sincronização" (not the old "No band sync yet")

  4. Confirm no crash on launch.

  5. Optional (if WHOOP is available): Connect the WHOOP band after 04:00 local time. Watch the Xcode console — you should see the historical sync trigger fire. The status should eventually change to "Sincronizado da pulseira" if enough gravity data exists.
     If WHOOP is not available: the trigger guard will be satisfied when the conditions match — this is not required for phase acceptance.
  </how-to-verify>

  <resume-signal>Type "approved" if the app builds and shows "A aguardar sincronização" in the Sleep V2 card. Describe any issue if the build fails or the string is incorrect.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness → bridge | cargo test calls bridge functions directly — sandboxed per-test SQLite (tempfile) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-08 | Information Disclosure | Simulator shows internal status strings | accept | "A aguardar sincronização" and "Sincronizado da pulseira" are user-visible status labels — intentional |
| T-50-SC | Tampering | npm/pip/cargo installs | accept | no new packages this plan |
</threat_model>

<verification>
Rust: cd /Users/francisco/Documents/goose/Rust/core && cargo test -p goose-core 2>&1 | grep -E "^test result"
Expected: "test result: ok. N passed; 0 failed; 0 ignored"

Swift: xcodebuild build → "Build succeeded"

Visual: SleepV2BandSyncCard shows "A aguardar sincronização" on first launch.
</verification>

<success_criteria>
1. cargo test -p goose-core: zero failures (all existing tests + 4 new tests green)
2. xcodebuild: "Build succeeded" (no compilation errors)
3. SleepV2BandSyncCard displays "A aguardar sincronização" on simulator launch (not "No band sync yet")
4. App does not crash on launch
5. Human approval received via checkpoint
</success_criteria>

<output>
Create .planning/phases/50-morning-band-sleep-sync/50-03-SUMMARY.md when done.
</output>

## Artifacts this phase produces

| Symbol / File | Type | Consumed by |
|---------------|------|-------------|
| Phase verification result | Human approval | gsd-verify-work gate |
| 50-03-SUMMARY.md | Planning artifact | v7.0 milestone completion tracking |
</output>
