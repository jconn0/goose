---
phase: 50-morning-band-sleep-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Rust/core/src/bridge.rs
  - Rust/core/tests/bridge_tests.rs
autonomous: true
requirements:
  - SLP-SYNC-01
  - SLP-SYNC-02

must_haves:
  truths:
    - "gravity_x/y/z from K24 V24History frames are extracted and persisted to the gravity table after upload.get_recent_decoded_streams"
    - "gravity2_x/y/z are inserted to gravity2_samples when present in V24History frames"
    - "cargo test passes: bridge_v24_gravity_extraction, bridge_v24_gravity_insert_roundtrip, bridge_band_sleep_external_session_insert, bridge_band_sleep_no_duplicate"
    - "K10 accel extraction is unaffected — no regression"
  artifacts:
    - path: "Rust/core/src/bridge.rs"
      provides: "V24History branch with gravity accumulation and store.insert_gravity_rows call"
      contains: "gravity2"
    - path: "Rust/core/tests/bridge_tests.rs"
      provides: "4 new tests for gravity extraction, roundtrip, external sleep insert, idempotency"
      contains: "bridge_v24_gravity_extraction"
  key_links:
    - from: "bridge.rs V24History branch"
      to: "store.insert_gravity_rows"
      via: "gravity vec collect + call after frame loop"
      pattern: "insert_gravity_rows"
    - from: "bridge_tests.rs"
      to: "upload.get_recent_decoded_streams bridge method"
      via: "call_bridge with import + query"
      pattern: "bridge_v24_gravity_extraction"
---

<objective>
Wire gravity_x/y/z extraction from V24History frames into the upload.get_recent_decoded_streams bridge — the only existing gap blocking overnight sleep staging. After this plan, any K18/K24 frame ingested during historical BLE sync will persist its gravity sample to SQLite, enabling metrics.sleep_staging to run Cole-Kripke 4-class staging on overnight data.

Purpose: SLP-SYNC-01 — gravity offsets 33–44 promoted from ignored fields to persisted data.
Output: Modified bridge.rs (V24History branch + post-loop insert), 4 new cargo tests green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-morning-band-sleep-sync/50-CONTEXT.md
@.planning/phases/50-morning-band-sleep-sync/50-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: V24History gravity extraction + gravity2 vec in bridge.rs</name>
  <files>Rust/core/src/bridge.rs</files>

  <read_first>
    Read Rust/core/src/bridge.rs lines 3304–3534 (upload_get_recent_decoded_streams_bridge function).
    Read Rust/core/src/bridge.rs lines 3367–3398 (K10 accel extraction pattern — reference for V24 analogue).
    Read Rust/core/src/bridge.rs lines 3412–3463 (V24History branch — current state with `..` ignoring gravity).
    Read Rust/core/src/store.rs lines 6542–6608 (insert_gravity_rows and insert_gravity2_batch signatures).
    Read Rust/core/src/protocol.rs lines 166–190 (DataPacketBodySummary::V24History struct with gravity fields).
    Read Rust/core/tests/v24_biometric_protocol_tests.rs lines 7–57 (make_82_byte_payload with gravity offsets confirmed: x=33, y=37, z=41, f32 LE; data offset = payload offset - 3).
  </read_first>

  <behavior>
    - After change: upload.get_recent_decoded_streams called on a DB with one K24 frame (gravity_x=0.98, gravity_y=0.01, gravity_z=0.05) returns result["gravity"] with length 1 and x≈0.98
    - After change: store.gravity_rows_between on same DB returns 1 row with x≈0.98 (gravity persisted, not just returned in JSON)
    - After change: K24 frame with NO gravity (all-zero gravity fields, but Option wraps them as None) produces 0 gravity entries — no panics
    - After change: gravity2_x/y/z fields (offsets 49–60) are collected into a separate gravity2 vec and inserted via store.insert_gravity2_batch
    - K10 accel behavior unchanged — existing tests still pass
  </behavior>

  <action>
  In upload_get_recent_decoded_streams_bridge (bridge.rs ~line 3323), add a gravity2 vec declaration immediately after the existing `let mut gravity` declaration:

    let mut gravity2: Vec<serde_json::Value> = Vec::new();

  In the DataPacketBodySummary::V24History match arm (~line 3412), change the destructuring from using `..` to explicitly bind gravity_x, gravity_y, gravity_z, gravity2_x, gravity2_y, gravity2_z (per D-01 in CONTEXT.md — "inline no V24History branch"). Add these bindings alongside the existing hr, rr_intervals_ms, skin_contact, spo2_red, spo2_ir, skin_temp_raw, resp_raw.

  After the existing resp section inside the V24History arm, add two gravity blocks:

  Block 1 — primary gravity (single sample per V24 frame — no loop needed unlike K10):
    If ts_unix, gravity_x (as Some(x)), gravity_y (as Some(y)), gravity_z (as Some(z)) are all Some:
      push json!({"ts": ts, "x": x as f64, "y": y as f64, "z": z as f64}) to gravity vec.
    Fields gravity_x/y/z are Option<f32> in the struct — use if let (Some(ts), Some(x), Some(y), Some(z)) = (ts_unix, *gravity_x, *gravity_y, *gravity_z) pattern.
    Do NOT apply IMU_LSB_PER_G conversion — V24History gravity fields are already in g units (f32), unlike K10 which uses i16 LSB.

  Block 2 — gravity2 (optional second triplet, bytes 49–60):
    Same pattern but binding gravity2_x, gravity2_y, gravity2_z from the struct.
    Push to gravity2 vec.

  After the frames loop ends (~line 3465) and before the `let result = json!(...)` block (~line 3519):
    Add insert calls using the store already open:
      let _ = store.insert_gravity_rows(&args.device_id, &gravity.iter().filter_map(|v| { ... }).collect::<Vec<_>>());
    However, gravity vec contains serde_json::Value objects. To call insert_gravity_rows which takes &[(f64,f64,f64,f64)], convert inline: iterate over gravity vec, extract ts/x/y/z as f64, collect to Vec<(f64,f64,f64,f64)>. Use v["ts"].as_f64(), v["x"].as_f64(), v["y"].as_f64(), v["z"].as_f64() with filter_map pattern to skip any that don't parse cleanly.
    Similarly call store.insert_gravity2_batch(&args.device_id, &gravity2_tuples).
    The UploadGetRecentDecodedStreamsArgs struct must have a device_id field — check the struct definition. If it doesn't have device_id yet, add `pub device_id: String` to UploadGetRecentDecodedStreamsArgs (search for the struct in bridge.rs). If device_id is already present, use it directly. If absent, skip the insert (log via eprintln) and add a TODO comment — do not panic.

  Add gravity2 to the result JSON in the existing `let result = json!(...)` block alongside the existing "gravity" key.

  Per D-01 (CONTEXT.md): device_id uses active_device_id passed to import_frame_batch — for upload_get_recent_decoded_streams_bridge use args.device_id.
  </action>

  <verify>
    <automated>cd /Users/francisco/Documents/goose/Rust/core && cargo check 2>&1 | tail -20</automated>
  </verify>

  <done>cargo check passes with zero errors. V24History arm no longer uses bare `..` for gravity fields — it binds gravity_x/y/z explicitly. gravity2 vec is declared and populated. store.insert_gravity_rows and store.insert_gravity2_batch are called after the frames loop.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 4 cargo tests for gravity V24 extraction, roundtrip, external sleep insert, idempotency</name>
  <files>Rust/core/tests/bridge_tests.rs</files>

  <read_first>
    Read Rust/core/tests/bridge_tests.rs lines 8850–8920 (historical_k18_frame_hex helper and surrounding context — use as template for K24 frame builder).
    Read Rust/core/tests/v24_biometric_protocol_tests.rs lines 7–103 (make_82_byte_payload layout — gravity_x at data offset 33, gravity_y at 37, gravity_z at 41, all f32 LE; payload[3+offset] = data[offset]).
    Read Rust/core/tests/bridge_tests.rs lines 1–30 (imports — check which helpers are already imported: build_v5_payload_frame, PACKET_TYPE_HISTORICAL_DATA, call_bridge, etc.).
    Read Rust/core/src/store.rs lines 4659–4716 (insert_external_sleep_session with idempotency constraint — understand platform_record_id UNIQUE usage).
    Read Rust/core/src/sleep_staging.rs lines 35, 97–118 (STAGING_METHOD_NO_IMU constant = "no_imu_data"; SleepStagingOutput fields).
  </read_first>

  <behavior>
    - Test bridge_v24_gravity_extraction: import one K24 frame via capture.import_frame_batch with gravity_x=0.98, gravity_y=0.01, gravity_z=0.05; call upload.get_recent_decoded_streams; assert result["gravity"].as_array().len() == 1 and result["gravity"][0]["x"].as_f64() is approximately 0.98
    - Test bridge_v24_gravity_insert_roundtrip: same setup + call store.gravity_rows_between for the device_id and a wide time window; assert the count is 1 (gravity persisted to DB, not just in JSON response)
    - Test bridge_band_sleep_external_session_insert: directly call sleep.import_external_history bridge with source="band_ble", platform="goose_ble", a deterministic sleep_id; assert result["inserted"] == 1
    - Test bridge_band_sleep_no_duplicate: call sleep.import_external_history twice with identical sleep_id and platform_record_id; assert second call returns result["unchanged"] == 1 (or inserted == 0), not an error
  </behavior>

  <action>
  Add a helper function historical_k24_frame_hex_with_gravity(gx: f32, gy: f32, gz: f32) -> String near the existing historical_k18_frame_hex helper:

  Build a payload Vec<u8> of size 3 + 79 = 82 bytes:
    payload[0] = PACKET_TYPE_HISTORICAL_DATA  (constant already imported)
    payload[1] = 24u8  (packet_k = 24)
    payload[2] = 1u8   (version)
    payload[3+0 .. 3+4] = timestamp_seconds as u32 LE — use a recognisable value like 1_000_000u32 so ts_unix is non-zero (≠ None)
    payload[3+33 .. 3+37] = gx.to_le_bytes()
    payload[3+37 .. 3+41] = gy.to_le_bytes()
    payload[3+41 .. 3+45] = gz.to_le_bytes()
    payload[3+48] = 1u8  (skin_contact = 1, so HR/SpO2 gates pass)
  Return hex::encode(build_v5_payload_frame(&payload)).

  Note: timestamp_seconds in DataPacket for V24History comes from data[0..4] u32 LE in the V24 body parser (protocol.rs). Confirm the exact offset by checking protocol.rs parse_v24_body_for_test or the V24History struct — if it's at a different offset, adjust. The ts_unix value in bridge.rs is timestamp_seconds.map(|s| s as f64). Setting data[0..4] to a non-zero u32 ensures ts_unix = Some(non-zero), so the gravity if-let passes.

  Test 1 — bridge_v24_gravity_extraction:
    Create a temp DB. Import one K24 frame via capture.import_frame_batch (use call_bridge with method="capture.import_frame_batch", args including device_id="test-device-01", active_device_id="test-device-01", frames=[{frame_hex: historical_k24_frame_hex_with_gravity(0.98, 0.01, 0.05), captured_at: current ISO8601 string, device_type: "WHOOP_4"}]).
    Call upload.get_recent_decoded_streams with device_id="test-device-01", since_ts=0.0.
    Assert: result["gravity"].as_array().unwrap().len() == 1.
    Assert: (result["gravity"][0]["x"].as_f64().unwrap() - 0.98f64).abs() < 0.001.

  Test 2 — bridge_v24_gravity_insert_roundtrip:
    Same import as Test 1 (new temp DB).
    After import+upload call, call store.gravity_rows_between bridge: method="store.gravity_rows_between", args: {database_path, device_id:"test-device-01", ts_start:0.0, ts_end:9_999_999_999.0}.
    Assert result["rows"].as_array().unwrap().len() == 1.
    Assert gravity row x value ≈ 0.98.

  Test 3 — bridge_band_sleep_external_session_insert:
    Create temp DB. Call sleep.import_external_history bridge with:
      sleep_id: "band_ble.test-device-01.2026-06-10"
      source: "band_ble"
      platform: "goose_ble"
      platform_record_id: "band_ble.test-device-01.2026-06-10"
      start_time_unix_ms: 1_718_000_000_000i64
      end_time_unix_ms: 1_718_029_200_000i64  (8h later)
      confidence: 0.7
      stage_summary: {} (empty ok)
      provenance: {"source":"band_ble","auto_sync":true}
    Assert result["inserted"].as_i64() == Some(1) (or result["ok"] == true).
    Check the exact response schema of sleep.import_external_history by reading bridge.rs around line 4136 or searching for "import_external_history" — use the correct key name.

  Test 4 — bridge_band_sleep_no_duplicate:
    Same as Test 3 but call sleep.import_external_history twice on the same DB with identical args.
    Assert second call does NOT return an error (result is Ok).
    Assert result["inserted"] == 0 OR result["unchanged"] == 1 — whatever the bridge returns for idempotent re-insert (check store.rs insert_external_sleep_session for the return value on conflict).

  Use the same call_bridge helper pattern used by existing tests. For captured_at ISO8601, use "2026-06-10T04:00:00.000Z" as a fixed string for test reproducibility.
  </action>

  <verify>
    <automated>cd /Users/francisco/Documents/goose/Rust/core && cargo test bridge_v24_gravity_extraction bridge_v24_gravity_insert_roundtrip bridge_band_sleep_external_session_insert bridge_band_sleep_no_duplicate 2>&1 | tail -30</automated>
  </verify>

  <done>All 4 named tests pass. cargo test -p goose-core shows 0 failures. The gravity extraction end-to-end path (import K24 frame → gravity in DB) is verified by automated test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BLE frames → bridge | Frames arrive via BLE; bridge.rs parses parsed_payload_json from decoded_frames — CRC validation already gates this |
| bridge → SQLite gravity table | gravity rows written by insert_gravity_rows with INSERT OR IGNORE — dedup at DB level |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-01 | Tampering | gravity table insert with empty device_id | mitigate | validate_required("device_id", ...) already in insert_gravity_rows (store.rs line 6547) — returns error, no insert |
| T-50-02 | Tampering | duplicate external_sleep_sessions for same night | mitigate | sleep_id deterministic (band_ble.{device_id}.{date}) + UNIQUE(platform, platform_record_id) constraint — idempotent |
| T-50-03 | Information Disclosure | gravity2 data inserted without analysis | accept | gravity2 is raw sensor data, low sensitivity; not used in scoring pipeline |
| T-50-SC | Tampering | npm/pip/cargo installs | accept | no new packages — zero installs this plan |
</threat_model>

<verification>
Full suite: cd /Users/francisco/Documents/goose/Rust/core && cargo test -p goose-core 2>&1 | tail -20

Gate: zero failures. No regression in K10 accel tests or V24 biometric protocol tests.
</verification>

<success_criteria>
1. V24History match arm in upload_get_recent_decoded_streams_bridge binds gravity_x/y/z and gravity2_x/y/z instead of ignoring with `..`
2. gravity vec populated with json!({"ts":..,"x":..,"y":..,"z":..}) entries for each V24 frame with non-None gravity
3. store.insert_gravity_rows called after frames loop with device_id from args
4. gravity2 vec populated and store.insert_gravity2_batch called
5. result JSON includes "gravity2" key alongside "gravity"
6. cargo test bridge_v24_gravity_extraction PASSES
7. cargo test bridge_v24_gravity_insert_roundtrip PASSES
8. cargo test bridge_band_sleep_external_session_insert PASSES
9. cargo test bridge_band_sleep_no_duplicate PASSES
10. Full cargo test suite green (no regression)
</success_criteria>

<output>
Create .planning/phases/50-morning-band-sleep-sync/50-01-SUMMARY.md when done.
</output>

## Artifacts this phase produces

| Symbol / File | Type | Consumed by |
|---------------|------|-------------|
| `bridge.rs` V24History gravity extraction | Rust code change | metrics.sleep_staging (reads gravity table populated by this) |
| `bridge.rs` gravity2 vec + insert_gravity2_batch call | Rust code change | Future gravity2 analysis |
| `store.insert_gravity_rows` call in upload bridge | Side effect | gravity table → sleep_staging pipeline |
| `bridge_tests.rs` `bridge_v24_gravity_extraction` | Test | CI green gate |
| `bridge_tests.rs` `bridge_v24_gravity_insert_roundtrip` | Test | CI green gate |
| `bridge_tests.rs` `bridge_band_sleep_external_session_insert` | Test | CI green gate |
| `bridge_tests.rs` `bridge_band_sleep_no_duplicate` | Test | CI green gate |
</output>
